<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>iptable的地址转换</title>
    <url>//2023/05/08/iptables/nat/</url>
    <content><![CDATA[<p><strong><font color='red'>如果想要NAT功能能够正常使用，需要开启Linux主机的核心转发功能。</font></strong></p>
<span id="more"></span>

<h2 id="SNAT"><a href="#SNAT" class="headerlink" title="SNAT"></a>SNAT</h2><p><em><strong>配置SNAT，可以隐藏网内主机的IP地址，也可以共享公网IP，访问互联网，如果只是共享IP的话，只配置如下SNAT规则即可</strong></em></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t nat -A POSTROUTING -s 10.1.0.0/16 -j SNAT --to-source 公网IP</span><br></pre></td></tr></table></figure>

<p><strong>如果公网IP是动态获取的，不是固定的，则可以使用MASQUERADE进行动态的SNAT操作。</strong></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将10.1网段的报文的源IP修改为eth0网卡中可用的地址。</span></span><br><span class="line">iptables -t nat -A POSTROUTING -s 10.1.0.0/16 -o eth0 -j MASQUERADE</span><br></pre></td></tr></table></figure>


<h2 id="DNAT"><a href="#DNAT" class="headerlink" title="DNAT"></a>DNAT</h2><p><em><strong>配置DNAT，可以通过公网IP访问局域网内的服务。</strong></em></p>
<p><strong><font color='red'>Tips:</font></strong> 理论上来说，只要配置DNAT规则，不需要对应的SNAT规则即可达到DNAT效果。但是测试过程中，对应SNAT规则也需要配置，才能正常DNAT，可以先尝试只配置DNAT规则，如果无法正常DNAT，再尝试添加对应的SNAT规则，SNAT规则配置一条即可，DNAT规则需要根据实际情况配置不同的DNAT规则。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t nat -I PREROUTING -d 公网IP -p tcp --dport 公网端口 -j DNAT --to-destination 私网IP:端口号</span><br><span class="line"></span><br><span class="line">iptables -t nat -I PREROUTING -d 公网IP -p tcp --dport 8080 -j DNAT --to-destination 10.1.0.1:80</span><br><span class="line"></span><br><span class="line">iptables -t nat -A POSTROUTING -s 10.1.0.0/16 -j SNAT --to-source 公网IP</span><br></pre></td></tr></table></figure>

<p><strong>在本机进行目标端口映射时可以使用REDIRECT动作</strong></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">本机80端口映射到8080</span></span><br><span class="line">iptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-ports 8080</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>linux</tag>
        <tag>iptables</tag>
      </tags>
  </entry>
  <entry>
    <title>Iptable基础知识</title>
    <url>//2023/05/08/iptables/%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p><em><strong>iptables可以检测、修改、转发、重定向和丢弃IPv4数据包</strong></em></p>
<p>过滤ipv4数据包的代码内置于linux内核中, 按照不同目的被组织成不同<strong>表</strong>的集合。<strong>表</strong>由一系列预先定义的<strong>链</strong>组成，<strong>链</strong> 包含规则。每一条规则谓词的潜在匹配和一个动作(<strong>目标</strong>), 如果该谓词为真则执行该动作。</p>
<span id="more"></span>

<p><img src="/images/tables_traverse.png"></p>
<h2 id="表-Tables"><a href="#表-Tables" class="headerlink" title="表(Tables)"></a>表(Tables)</h2><ul>
<li><strong><code>raw</code></strong> 用于配置数据包, <strong><code>raw</code></strong> 中的数据包不会被系统跟踪</li>
<li><strong><code>filter</code></strong> 用于存放所有与防火墙相关操作的默认表</li>
<li><strong><code>nat</code></strong> 用于网络地址转换</li>
<li><strong><code>mangle</code></strong> 用于对特定数据包的修改</li>
<li><strong><code>security</code></strong> 用于强制访问控制网络规则</li>
</ul>
<h3 id="规则表之间的顺序："><a href="#规则表之间的顺序：" class="headerlink" title="规则表之间的顺序："></a>规则表之间的顺序：</h3><p>​<code>raw</code>→<code>mangle</code>→<code>nat</code>→<code>filter</code>，即先做<code>状态跟踪</code>→<code>在做标记</code>→<code>在做修改源目IP或端口</code>→<code>在做是否过滤</code></p>
<h2 id="链-Chains"><a href="#链-Chains" class="headerlink" title="链(Chains)"></a>链(Chains)</h2><p><em><strong>内置的链有默认规则</strong></em></p>
<p>​表由链组成，链是一些按顺序排列的规则的列表。默认的 <strong><code>filter</code></strong> 表包含 <strong><code>INPUT</code></strong>, <strong><code>OUTPUT</code></strong>, <strong><code>FORWARD</code></strong> 3条内建的 链。这三条链作用于数据包过滤过程中的不同时间点。 <strong><code>nat</code></strong>  表包含PREROUTING,POSTROUTING和OUTPUT链。</p>
<p>​默认情况下，任何链中都没有规则。可以向链中添加自己想要的规则。链的默认规则通常设置为 <strong><code>ACCEPT</code></strong> ，如果想要确保任何数据包都不能通过规则集,则修改为 <strong><code>DROP</code></strong> 。默认的规则总是在链的最后生效。</p>
<h3 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h3><ul>
<li><strong><code>raw</code></strong> ：包含了 <strong><code>PREROUTING</code></strong> 和 <strong><code>OUTPUT</code></strong> ，用于跟踪报文</li>
<li><strong><code>mangle</code></strong> ：包含了 <strong><code>PREROUTING</code></strong> 、 <strong><code>INPUT</code></strong> 、 <strong><code>FORWARD</code></strong> 、 <strong><code>OUTPUT</code></strong> 、 <strong><code>POSTROUTING</code></strong> 。用于修改报文</li>
<li><strong><code>nat</code></strong> ：包含了 <strong><code>PREROUTING</code></strong> 、 <strong><code>OUTPUT</code></strong> 、 <strong><code>POSTROUTING</code></strong> 。用于修改ip或者端口</li>
<li><strong><code>filter</code></strong> ：包含了 <strong><code>INPUT</code></strong> 、 <strong><code>FORWARD</code></strong> 、 <strong><code>OUTPUT</code></strong> 。用于过滤数据</li>
</ul>
<h3 id="规则链之间的顺序"><a href="#规则链之间的顺序" class="headerlink" title="规则链之间的顺序"></a>规则链之间的顺序</h3><ul>
<li><strong>入站</strong>：PREROUTING→INPUT 路由前发现是自己的，直接进站</li>
<li><strong>出站</strong>：OUTPUT→POSTROUTING</li>
<li><strong>转发</strong>：PREOUTING→FORWARD→POSTROUTING</li>
</ul>
<p><strong><font color='red'>Tips</font></strong> ： <strong><code>PREROUTING</code></strong> 和 <strong><code>POSTROUTING</code></strong> 是最外围，规则链是靠时机分的，分为了入站，出站，转发三个时机</p>
<h3 id="规则链内的匹配顺序："><a href="#规则链内的匹配顺序：" class="headerlink" title="规则链内的匹配顺序："></a>规则链内的匹配顺序：</h3><ul>
<li>按顺序依次检查，匹配即停止（LOG策略外）</li>
<li>若找不到相匹配规则，按该链的默认策略处理</li>
</ul>
<h2 id="规则-Rules"><a href="#规则-Rules" class="headerlink" title="规则(Rules)"></a>规则(Rules)</h2><p>​数据包的过滤基于<strong>规则</strong>。规则由一个目标(数据包匹配一条条件后的动作)和很多匹配(导致该规则可以应用的数据包所满足的条件)指定。一个规则的典型匹配事项是数据包进入的网络接口(例如<em>eht0</em>)、数据包的类型(ICMP、TCP、或者UDP)和数据包的目的端口</p>
<p>​目标使用 <strong><font color='red'>-j</font></strong> 或者 <font color='red'><strong>–jump</strong></font> 选项指定。目标可以是用户的自定义链(例如 ，如果条件匹配，跳转到之后的用户自定义的链，继续处理)、一个内置的目标或者是一个目标扩展。内置的目标是 <strong><code>ACCEPT</code></strong> 、 <strong><code>DROP</code></strong> 、 <strong><code>QUEUE</code></strong> 和 <strong><code>RETURN</code></strong> ，目标扩展是 <strong><code>REJECT</code></strong> 或者 <strong><code>LOG</code></strong> 。如果是目标是内置目标，数据包的命运会理解被决定并且在当前表的数据包的处理过程 会停止。如果目标是用户自定义的链，并且数据包成功穿过第二条链，目标将移动到原始链的下一条规则。目标扩展可以被 <em>终止</em> 或者 <em>不终止</em></p>
<h2 id="遍历链-Traversing-Chains"><a href="#遍历链-Traversing-Chains" class="headerlink" title="遍历链(Traversing Chains)"></a>遍历链(Traversing Chains)</h2><p>​网络流程图描述了链在任何接口上收到的网络数据包是按照怎样的顺序穿过表的流量控制链。第一个路由策略包括决定数据包的目的地址是本机(这种情况下，数据包穿过 <strong><code>INPUT</code></strong> 链)，还是其他主机(数据包穿过 <strong><code>FORWARD</code></strong> 链)；中间的路由策略包括决定给传出的数据包使用哪个源地址，分配哪个网络接口(网卡)；最后一个路由策略存在是因为先前的mangle与nat链可能会修改数据包的路由信息。数据包通过路径上的每一条链时， 链中的每一条规则按<strong>顺序匹配</strong>；无论何时匹配了一条规则，相应的 target&#x2F;jump 动作将会执行。</p>
<p>​在任何时候，若 <strong><code>DROP</code></strong> target 的规则实现完全匹配，那么被匹配的数据包会被丢弃，不会进行进一步处理。如果一个数据包在 链中被 <strong><code>ACCEPT</code></strong> ，那么它也会被所有的父链 <strong><code>ACCEPT</code></strong> ，并且不再遍历其他父链。然而，要注意的是，数据包还会以正常的方式继续遍历其他表中的其他链。</p>
<h2 id="模块-Modules"><a href="#模块-Modules" class="headerlink" title="模块(Modules)"></a>模块(Modules)</h2><p>​               有许多模块可以扩展iptables。例如conntrack，limit</p>
]]></content>
      <tags>
        <tag>linux</tag>
        <tag>iptables</tag>
      </tags>
  </entry>
  <entry>
    <title>Iptables自定义链</title>
    <url>//2023/05/08/iptables/%E8%87%AA%E5%AE%9A%E4%B9%89%E9%93%BE/</url>
    <content><![CDATA[<h2 id="创建自定义链"><a href="#创建自定义链" class="headerlink" title="创建自定义链"></a>创建自定义链</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">在filter表中创建IN_WEB自定义链</span></span><br><span class="line">iptables -t filter -N IN_WEB</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="引用自定义链"><a href="#引用自定义链" class="headerlink" title="引用自定义链"></a>引用自定义链</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">在INPUT链中引用刚才创建的自定义链</span></span><br><span class="line">iptables -t filter -I INPUT -p tcp --dport 80 -j IN_WEB</span><br></pre></td></tr></table></figure>


<h2 id="重命名自定义链"><a href="#重命名自定义链" class="headerlink" title="重命名自定义链"></a>重命名自定义链</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将IN_WEB自定义链重命名为WEB</span></span><br><span class="line">iptables -E IN_WEB WEB</span><br></pre></td></tr></table></figure>


<h2 id="删除自定义链"><a href="#删除自定义链" class="headerlink" title="删除自定义链"></a>删除自定义链</h2><p><strong>删除自定义链需要满足一下两个条件:</strong></p>
<ul>
<li>自定义链没有被引用</li>
<li>自定义链中没有任何规则</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">删除引用计数为0并且不包含任何规则的WEB链</span></span><br><span class="line">iptables -X WEB</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>linux</tag>
        <tag>iptables</tag>
      </tags>
  </entry>
  <entry>
    <title>Iptable规则匹配条件</title>
    <url>//2023/05/08/iptables/%E8%A7%84%E5%88%99%E5%8C%B9%E9%85%8D%E6%9D%A1%E4%BB%B6/</url>
    <content><![CDATA[<h2 id="源地址匹配"><a href="#源地址匹配" class="headerlink" title="源地址匹配"></a>源地址匹配</h2><p><em><strong><code>-s</code> 用于匹配报文的源地址,可以同时指定多个源地址，每个IP之间用逗号隔开，也可以指定为一个网段</strong></em></p>
<span id="more"></span>

<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">丢掉来自192.168.1.111和192.168.1.118的报文</span></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.111,192.168.1.118 -j DROP</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">接收来自192.168.1.0/24网段的报文</span></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.0/24 -j ACCEPT</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">接收非192.168.1.0/24网段的报文</span></span><br><span class="line">iptables -t filter -I INPUT ! -s 192.168.1.0/24 -j ACCEPT</span><br></pre></td></tr></table></figure>


<h2 id="目标地址"><a href="#目标地址" class="headerlink" title="目标地址"></a>目标地址</h2><p><em><strong><code>-d</code>用于匹配报文的目标地址,可以同时指定多个目标地址，每个IP之间用逗号隔开，也可以指定为一个网段</strong></em></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">丢掉发往192.168.1.111和192.168.1.118的报文</span></span><br><span class="line">iptables -t filter -I OUTPUT -d 192.168.1.111,192.168.1.118 -j DROP</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">接收发往192.168.1.0/24网段的报文</span></span><br><span class="line">iptables -t filter -I INPUT -d 192.168.1.0/24 -j ACCEPT</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">接收发往非192.168.1.0/24网段的报文</span></span><br><span class="line">iptables -t filter -I INPUT ! -d 192.168.1.0/24 -j ACCEPT</span><br></pre></td></tr></table></figure>


<h2 id="协议类型"><a href="#协议类型" class="headerlink" title="协议类型"></a>协议类型</h2><p><em><strong><code>-p</code>用于匹配报文的协议类型,可以匹配的协议类型tcp、udp、udplite、icmp、esp、ah、sctp等</strong></em></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">接收来自192.168.1.146的TCP报文</span></span><br><span class="line">iptables -t filter -I INPUT -p tcp -s 192.168.1.146 -j ACCEPT</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">接收来自非192.168.1.146的UDP报文</span></span><br><span class="line">iptables -t filter -I INPUT ! -p udp -s 192.168.1.146 -j ACCEPT</span><br></pre></td></tr></table></figure>


<h2 id="网卡接口"><a href="#网卡接口" class="headerlink" title="网卡接口"></a>网卡接口</h2><ul>
<li>入网流量</li>
</ul>
<p><em><strong><code>-i</code>用于匹配报文是从哪个网卡接口流入本机的，由于匹配条件只是用于匹配报文流入的网卡，所以在OUTPUT链与POSTROUTING链中不能使用此选项</strong></em></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">丢掉来自eth4网络接口的icmp协议报文</span></span><br><span class="line">iptables -t filter -I INPUT -p icmp -i eth4 -j DROP</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">丢掉非eth4网络接口的icmp协议报文</span></span><br><span class="line">iptables -t filter -I INPUT -p icmp ! -i eth4 -j DROP</span><br></pre></td></tr></table></figure>

<ul>
<li>出网流量</li>
</ul>
<p><em><strong><code>-o</code>用于匹配报文将要从哪个网卡接口流出本机，于匹配条件只是用于匹配报文流出的网卡，所以在INPUT链与PREROUTING链中不能使用此选项。</strong></em></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t filter -I OUTPUT -p icmp -o eth4 -j DROP</span><br><span class="line">iptables -t filter -I OUTPUT -p icmp ! -o eth4 -j DROP</span><br></pre></td></tr></table></figure>


<h1 id="扩展匹配条件"><a href="#扩展匹配条件" class="headerlink" title="扩展匹配条件"></a>扩展匹配条件</h1><h2 id="TCP扩展"><a href="#TCP扩展" class="headerlink" title="TCP扩展"></a>TCP扩展</h2><ul>
<li><code>-p tcp -m tcp –-sport</code> 用于匹配<strong>tcp</strong>协议报文的源端口，可以使用冒号指定一个连续的端口范围</li>
<li><code>-p tcp -m tcp –-dport</code> 用于匹配<strong>tcp</strong>协议报文的目标端口，可以使用冒号指定一个连续的端口范围</li>
<li><code>-p tcp -m tcp –tcp-flags</code> 用于匹配<strong>tcp</strong>协议报文的标志位</li>
<li><code>-p tcp -m tcp –syn</code> 用于匹配<strong>tcp</strong>新建连接的请求报文</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">拒绝源端口22且目的地址192.168.1.146的TCP协议报文</span></span><br><span class="line">iptables -t filter -I OUTPUT -d 192.168.1.146 -p tcp -m tcp --sport 22 -j REJECT</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">拒绝源地址192.168.1.146且目标端口22-25的TCP协议报文</span></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p tcp -m tcp --dport 22:25 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p tcp -m tcp --dport :22 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p tcp -m tcp --dport 80: -j REJECT</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">接受目标地址192.168.1.146且源端口非22的TCP协议报文</span></span><br><span class="line">iptables -t filter -I OUTPUT -d 192.168.1.146 -p tcp -m tcp ! --sport 22 -j ACCEPT</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">拒绝目标端口22且拥有SYN,ACK,FIN,RST,URG,PSH SYN标志的TCP报文</span></span><br><span class="line">iptables -t filter -I INPUT -p tcp -m tcp --dport 22 --tcp-flags SYN,ACK,FIN,RST,URG,PSH SYN -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -p tcp -m tcp --dport 22 --tcp-flags ALL SYN -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -p tcp -m tcp --dport 22 --syn -j REJECT</span><br></pre></td></tr></table></figure>


<h2 id="UDP扩展"><a href="#UDP扩展" class="headerlink" title="UDP扩展"></a>UDP扩展</h2><ul>
<li><code>-p udp -m udp --sport</code> 用于匹配<strong>udp</strong>协议报文的源端口，可以使用冒号指定一个连续的端口范围</li>
<li><code>-p udp -m udp -–dport</code> 用于匹配<strong>udp</strong>协议报文的目标端口，可以使用冒号指定一个连续的端口范围</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t filter -I OUTPUT -d 192.168.1.146 -p udp -m udp --sport 22 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p udp -m udp --dport 22:25 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p udp -m udp --dport :22 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p udp -m udp --dport 80: -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -d 192.168.1.146 -p udp -m udp ! --sport 22 -j ACCEPT</span><br></pre></td></tr></table></figure>


<h2 id="MultiPort扩展"><a href="#MultiPort扩展" class="headerlink" title="MultiPort扩展"></a>MultiPort扩展</h2><ul>
<li><code>-p tcp -m multiport –-sports</code> 用于匹配报文的源端口，可以指定离散的多个端口号,端口之间用”逗号”隔开</li>
<li><code>-p udp -m multiport –-dports</code> 用于匹配报文的目标端口，可以指定离散的多个端口号，端口之间用”逗号”隔开</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t filter -I OUTPUT -d 192.168.1.146 -p udp -m multiport --sports 137,138 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p tcp -m multiport --dports 22,80 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p tcp -m multiport ! --dports 22,80 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p tcp -m multiport --dports 80:88 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -p tcp -m multiport --dports 22,80:88 -j REJECT</span><br></pre></td></tr></table></figure>


<h2 id="iprange扩展"><a href="#iprange扩展" class="headerlink" title="iprange扩展"></a>iprange扩展</h2><p><em><strong>IP段的始末IP使用”横杠”连接，<code>–src-range</code>与<code>–dst-range</code>和其他匹配条件一样，能够使用”!”取反</strong></em></p>
<ul>
<li><code>--src-range</code> 匹配报文的源地址所在范围</li>
<li><code>--dst-range</code> 匹配报文的目的地址所在范围</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t filter -I INPUT -m iprange --src-range 192.168.1.127-192.168.1.146 -j DROP</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -m iprange --dst-range 192.168.1.127-192.168.1.146 -j DROP</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -m iprange ! --src-range 192.168.1.127-192.168.1.146 -j DROP</span><br></pre></td></tr></table></figure>


<h2 id="string扩展"><a href="#string扩展" class="headerlink" title="string扩展"></a>string扩展</h2><p><em><strong>可以指定要匹配的字符串，如果报文中包含对应的字符串，则符合匹配条件</strong></em></p>
<ul>
<li><code>–algo</code>：  用于指定匹配算法，可选的算法有bm与kmp</li>
<li><code>–string</code>：指定需要匹配的字符串</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t filter -I INPUT -p tcp --sport 80 -m string --algo bm --string &quot;OOXX&quot; -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -p tcp --sport 80 -m string --algo bm --string &quot;OOXX&quot; -j REJECT</span><br></pre></td></tr></table></figure>


<h2 id="time扩展"><a href="#time扩展" class="headerlink" title="time扩展"></a>time扩展</h2><ul>
<li><code>–timestart</code>：用于指定时间范围的开始时间，不可取反</li>
<li><code>–timestop</code>：用于指定时间范围的结束时间，不可取反</li>
<li><code>–weekdays</code>：用于指定”星期几”，可取反</li>
<li><code>–monthdays</code>：用于指定”几号”，可取反</li>
<li><code>–datestart</code>：用于指定日期范围的开始日期，不可取反</li>
<li><code>–datestop</code>：用于指定日期范围的结束时间，不可取反</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t filter -I OUTPUT -p tcp --dport 80 -m time --timestart 09:00:00 --timestop 19:00:00 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p tcp --dport 443 -m time --timestart 09:00:00 --timestop 19:00:00 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p tcp --dport 80  -m time --weekdays 6,7 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p tcp --dport 80  -m time --monthdays 22,23 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p tcp --dport 80  -m time ! --monthdays 22,23 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p tcp --dport 80  -m time --timestart 09:00:00 --timestop 18:00:00 --weekdays 6,7 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p tcp --dport 80  -m time --weekdays 5 --monthdays 22,23,24,25,26,27,28 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p tcp --dport 80  -m time --datestart 2017-12-24 --datestop 2017-12-27 -j REJECT</span><br></pre></td></tr></table></figure>


<h2 id="connlimit扩展"><a href="#connlimit扩展" class="headerlink" title="connlimit扩展"></a>connlimit扩展</h2><ul>
<li><code>–connlimit-above</code>：单独使用此选项时，表示限制每个IP的链接数量。</li>
<li><code>–connlimit-mask</code>：此选项不能单独使用，在使用–connlimit-above选项时，配合此选项，则可以针对”某类IP段内的一定数量的IP”进行连接数量的限制</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -I INPUT -p tcp --dport 22 -m connlimit --connlimit-above 2 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -I INPUT -p tcp --dport 22 -m connlimit --connlimit-above 20 --connlimit-mask 24 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -I INPUT -p tcp --dport 22 -m connlimit --connlimit-above 10 --connlimit-mask 27 -j REJECT</span><br></pre></td></tr></table></figure>


<h2 id="limit扩展"><a href="#limit扩展" class="headerlink" title="limit扩展"></a>limit扩展</h2><ul>
<li><code>–limit-burst</code>：类比”令牌桶”算法，此选项用于指定令牌桶中令牌的最大数量。</li>
<li><code>–limit</code>：类比”令牌桶”算法，此选项用于指定令牌桶中生成新令牌的频率，可用时间单位有second、minute 、hour、day。</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t filter -I INPUT -p icmp -m limit --limit-burst 3 --limit 10/minute -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -t filter -A INPUT -p icmp -j REJECT</span><br></pre></td></tr></table></figure>

<h2 id="icmp扩展"><a href="#icmp扩展" class="headerlink" title="icmp扩展"></a>icmp扩展</h2><p><em><strong>ICMP协议的全称为Internet Control Message Protocol，翻译为互联网控制报文协议，它主要用于探测网络上的主机是否可用，目标是否可达，网络是否通畅，路由是否可用等。</strong></em></p>
<ul>
<li><code>–icmp-type</code>：匹配icmp报文的具体类型</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables -t filter -I INPUT -p icmp -m icmp --icmp-type 8/0 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -p icmp --icmp-type 8 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p icmp -m icmp --icmp-type 0/0 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p icmp --icmp-type 0 -j REJECT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -p icmp --icmp-type &quot;echo-request&quot; -j REJECT</span><br></pre></td></tr></table></figure>


<h2 id="state扩展"><a href="#state扩展" class="headerlink" title="state扩展"></a>state扩展</h2><p><em><strong>对于state模块的连接而言，”连接”其中的报文可以分为5种状态，报文状态可以为NEW、ESTABLISHED、RELATED、INVALID、UNTRACKED</strong></em></p>
<ul>
<li><strong>NEW</strong>: 连接中的第一个包，状态就是NEW</li>
<li><strong>ESTABLISHED</strong>: 把NEW状态包后面的包的状态理解为ESTABLISHED，表示连接已建立。</li>
<li><strong>RELATED</strong>:</li>
<li><strong>INVALID</strong>：如果一个包没有办法被识别，或者这个包没有任何状态</li>
<li><strong>UNTRACKED</strong>：报文的状态为untracked时，表示报文未被追踪，当报文的状态为Untracked时通常表示无法找到相关的连接。</li>
</ul>
]]></content>
      <tags>
        <tag>linux</tag>
        <tag>iptables</tag>
      </tags>
  </entry>
  <entry>
    <title>Iptable规则管理</title>
    <url>//2023/05/08/iptables/%E8%A7%84%E5%88%99%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<p>规则（rules）其实就是网络管理员预定义的条件，规则一般的定义为“如果数据包头符合这样的条件，就这样处理这个数据包”。规则存储在内核空间的信息 包过滤表中，这些规则分别指定了源地址、目的地址、传输协议（如TCP、UDP、ICMP）和服务类型（如HTTP、FTP和SMTP）等。当数据包与规 则匹配时，iptables就根据规则所定义的方法来处理这些数据包，如放行（accept）、拒绝（reject）和丢弃（drop）等。配置防火墙的 主要工作就是添加、修改和删除这些规则。</p>
<span id="more"></span>

<h2 id="规则查询"><a href="#规则查询" class="headerlink" title="规则查询"></a>规则查询</h2><ul>
<li>字段解释<br><img src="/images/%E8%A7%84%E5%88%99%E5%AD%97%E6%AE%B5.png"></li>
</ul>
<table>
<thead>
<tr>
<th align="left">规则字段</th>
<th align="left">含义</th>
</tr>
</thead>
<tbody><tr>
<td align="left">pkts</td>
<td align="left">对应规则匹配到的报文个数</td>
</tr>
<tr>
<td align="left">bytes</td>
<td align="left">对应匹配到的报文包的大小总和</td>
</tr>
<tr>
<td align="left">target</td>
<td align="left">规则对应的target，往往表示匹配后的“动作”，即规则匹配成功后采取的操作</td>
</tr>
<tr>
<td align="left">prot</td>
<td align="left">规则对应的协议，是否只针对某些协议应用此规则</td>
</tr>
<tr>
<td align="left">opt</td>
<td align="left">规则对应的选项</td>
</tr>
<tr>
<td align="left">in</td>
<td align="left">数据包由哪个接口(网卡)流入，可以控制处理哪个网卡流入的报文</td>
</tr>
<tr>
<td align="left">out</td>
<td align="left">数据包由哪个接口(网卡)流出，可以控制处理哪个网卡流出的报文</td>
</tr>
<tr>
<td align="left">source</td>
<td align="left">规则对应的源地址，可以是一个IP，也可以是一个网段</td>
</tr>
<tr>
<td align="left">destination</td>
<td align="left">规则对应的目标地址，可以是一个IP，也可以是一个网段</td>
</tr>
</tbody></table>
<ul>
<li>链数据显示</li>
</ul>
<p><img src="/images/%E9%93%BE%E5%AD%97%E6%AE%B5.png"></p>
<table>
<thead>
<tr>
<th>名称</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>policy</td>
<td>当前链的默认规则</td>
</tr>
<tr>
<td>packets</td>
<td>当前链默认策略匹配到的包的数量</td>
</tr>
<tr>
<td>bytes</td>
<td>当前链默认策略匹配到的所有包的大小总和</td>
</tr>
</tbody></table>
<h2 id="添加规则"><a href="#添加规则" class="headerlink" title="添加规则"></a>添加规则</h2><p><strong><font color='red'>Tips:</font></strong> 规则顺序很重要。如果报文已经被前面的规则匹配到，iptables则会对报文执行对应的动作，即使后面的规则也能匹配到当前报文，很有可能也没有机会再对报文执行相应的动作。</p>
<ul>
<li>在对应链的末尾添加规则</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">iptables -t 表名 -A 链名 匹配条件 -j 动作</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">-A 在链的末尾添加规则</span></span><br><span class="line"></span><br><span class="line">iptables -t filter -A INPUT -s 192.168.1.146 -j DROP`</span><br></pre></td></tr></table></figure>

<ul>
<li>在对应链的开头添加规则</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">iptables -t 表名 -I 链名 匹配条件 -j 动作</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">-I 表示在链的开头添加规则</span></span><br><span class="line">iptables -t filter -I INPUT -s 192.168.1.146 -j ACCEPT</span><br></pre></td></tr></table></figure>

<ul>
<li>指定位置添加一条规则</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">iptables -t 表名 -I 链名 规则序号 匹配条件 -j 动作</span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">在第五个位置插入规则</span></span><br><span class="line">iptables -t filter -I INPUT 5 -s 192.168.1.146 -j REJECT</span><br></pre></td></tr></table></figure>


<h2 id="更新规则"><a href="#更新规则" class="headerlink" title="更新规则"></a>更新规则</h2><ul>
<li>根据规则编号修改</li>
</ul>
<p><font color='red'><strong>Tips</strong>:</font> 如果使用-R选项修改规则中的动作，那么必须指明原规则中的原匹配条件，例如源IP，目标IP等</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">iptables -t 表名 -R 链名 规则序号 规则原本的匹配条件 -j 动作</span></span><br><span class="line">iptables -t filter -R INPUT 3 -s 192.168.1.146 -j ACCEPT</span><br></pre></td></tr></table></figure>

<ul>
<li>修改表的默认策略</li>
</ul>
<p><font color='red'><strong>Tips</strong>:</font> 修改指定表的指定链的默认策略（默认动作），并非修改规则</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">iptables -t 表名 -P 链名 动作</span></span><br><span class="line"></span><br><span class="line">iptables -t filter -P FORWARD ACCEPT</span><br></pre></td></tr></table></figure>


<h2 id="删除规则"><a href="#删除规则" class="headerlink" title="删除规则"></a>删除规则</h2><ul>
<li>根据规则编号删除</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">iptables -t 表名 -D 链名 规则序号</span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">删除INPUT链中第三条规则</span></span><br><span class="line">iptables -t filter -D INPUT 3 </span><br></pre></td></tr></table></figure>

<ul>
<li>根据具体的匹配条件与动作删除规则</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash"> iptables -t 表名 -D 链名 匹配条件 -j 动作</span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">删除INPUT链中源地址为192.168.1.146，动作为ACCEPT的规则</span></span><br><span class="line">iptables -t filter -D INPUT -s 192.168.1.146 -j ACCEPT</span><br></pre></td></tr></table></figure>

<ul>
<li>删除所有规则</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">iptables -t 表名 -F</span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">清除filter表中所有规则</span></span><br><span class="line">iptables -t filter -F</span><br></pre></td></tr></table></figure>


<h2 id="保存规则"><a href="#保存规则" class="headerlink" title="保存规则"></a>保存规则</h2><ul>
<li>保存规则</li>
</ul>
<p><em><strong>保存规则命令如下，表示将iptables规则保存至&#x2F;etc&#x2F;sysconfig&#x2F;iptables文件中</strong></em></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">service iptables save</span><br></pre></td></tr></table></figure>
<ul>
<li>加载规则</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">iptables-restore &lt; /etc/sysconfig/iptables</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>linux</tag>
        <tag>iptables</tag>
      </tags>
  </entry>
  <entry>
    <title>kubernetes集群部署(一):初始化</title>
    <url>//2020/02/05/k8s/kubernetes%E9%9B%86%E7%BE%A4%E9%83%A8%E7%BD%B2(%E4%B8%80)_%E6%9C%BA%E5%99%A8%E5%88%9D%E5%A7%8B%E5%8C%96/</url>
    <content><![CDATA[<h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><p><strong>关闭防火墙和SELINUX</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">systemctl stop firewalld &amp;&amp; systemctl disable firewalld</span><br><span class="line">setenforce 0</span><br><span class="line">sed -i s/SELINUX=enforcing/SELINUX=disabled/ /etc/selinux/config</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="kubernetes结点初始化"><a href="#kubernetes结点初始化" class="headerlink" title="kubernetes结点初始化"></a>kubernetes结点初始化</h2><p><strong>关闭swap</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">swapoff -a &amp;&amp; sysctl -w vm.swappiness=0</span><br><span class="line">sed -i &#x27;/ swap / s/^\(.*\)$/#\1/g&#x27; /etc/fstab</span><br></pre></td></tr></table></figure>

<p><strong>安装docker</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">curl https://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo -o /etc/yum.repos.d/docker-ce.repo</span><br><span class="line">yum install docker-ce-18.06.2.ce-3.el7 -y</span><br><span class="line">systemctl start docker &amp;&amp; systemctl enable docker</span><br></pre></td></tr></table></figure>

<p><strong>内存参数</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">modprobe br_netfilter</span><br><span class="line"></span><br><span class="line">cat &lt;&lt; EOF | tee /etc/sysctl.d/k8s.conf</span><br><span class="line">net.ipv4.ip_forward = 1</span><br><span class="line">net.bridge.bridge-nf-call-ip6tables = 1</span><br><span class="line">net.bridge.bridge-nf-call-iptables = 1</span><br><span class="line">net.ipv4.tcp_tw_recycle=0</span><br><span class="line">vm.swappiness=0</span><br><span class="line">vm.overcommit_memory=1</span><br><span class="line">vm.panic_on_oom=0</span><br><span class="line">fs.inotify.max_user_watches=89100</span><br><span class="line">fs.file-max=52706963</span><br><span class="line">fs.nr_open=52706963</span><br><span class="line">net.ipv6.conf.all.disable_ipv6=1</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line">sysctl -p /etc/sysctl.d/k8s.conf</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>kubernetes集群部署(三):flannel集群</title>
    <url>//2020/02/09/k8s/kubernetes%E9%9B%86%E7%BE%A4%E9%83%A8%E7%BD%B2(%E4%B8%89)_flannel/</url>
    <content><![CDATA[<p><strong>安装flannel服务</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wget https://github.com/coreos/flannel/releases/download/v0.11.0/ \ </span><br><span class="line">    flannel-v0.11.0-linux-amd64.tar.gz</span><br><span class="line">mv flanneld mk-docker-opts.sh /usr/bin/</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<p><strong>配置文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mkdir /etc/flannel</span><br><span class="line">cat &lt;&lt;EOF &gt; /etc/flannel/flannel.conf</span><br><span class="line"># --etcd-endpoints是etcd结点连接信息</span><br><span class="line">FLANNEL_OPTIONS=&quot;--etcd-endpoints=https://10.0.2.15:2379, \</span><br><span class="line">    https://10.0.2.4:2379,https://10.0.2.5:2379 \</span><br><span class="line">    -etcd-cafile=/etc/etcd/ssl/ca.pem \</span><br><span class="line">    -etcd-certfile=/etc/etcd/ssl/server.pem \</span><br><span class="line">    -etcd-keyfile=/etc/etcd/ssl/server-key.pem&quot;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>


<p><strong>systemctl service文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; \EOF | tee /lib/systemd/system/flanneld.service</span><br><span class="line">[Unit]</span><br><span class="line">Description=Flanneld overlay address etcd agent</span><br><span class="line">After=network-online.target network.target etcd.service</span><br><span class="line">Before=docker.service</span><br><span class="line">[Service]</span><br><span class="line">Type=notify</span><br><span class="line">EnvironmentFile=/etc/flannel/flannel.conf</span><br><span class="line">ExecStart=/usr/bin/flanneld --ip-masq $FLANNEL_OPTIONS</span><br><span class="line">ExecStartPost=/usr/bin/mk-docker-opts.sh -k DOCKER_NETWORK_OPTIONS -d /run/flannel/subnet.env</span><br><span class="line">Restart=on-failure</span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<p><strong>修改docker.service文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">添加: EnvironmentFile=/run/flannel/subnet.env</span><br><span class="line"># $ARGS是docker启动参数</span><br><span class="line">ExecStart命令更新: /usr/bin/dockerd $DOCKER_NETWORK_OPTIONS $ARGS</span><br></pre></td></tr></table></figure>

<p><strong>向etcd中写入POD网段信息</strong></p>
<p><em>flannel读取etcd中的pod网段信息,用于设置docker0网桥</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">etcdctl \</span><br><span class="line">--ca-file=/etc/etcd/ssl/ca.pem --cert-file=/etc/etcd/ssl/server.pem \</span><br><span class="line">--key-file=/etc/etcd/ssl/server-key.pem \</span><br><span class="line">--endpoints=&quot;https://10.0.2.15:2379&quot; \</span><br><span class="line">set /coreos.com/network/config  &#x27;&#123; &quot;Network&quot;: &quot;172.20.0.0/16&quot;, &quot;Backend&quot;: &#123;&quot;Type&quot;: &quot;vxlan&quot;&#125;&#125;&#x27;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>kubernetes机器部署(四):k8s集群部署</title>
    <url>//2020/02/09/k8s/kubernetes%E9%9B%86%E7%BE%A4%E9%83%A8%E7%BD%B2(%E5%9B%9B)_k8s%E9%9B%86%E7%BE%A4%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h2 id="获取二进制文件"><a href="#获取二进制文件" class="headerlink" title="获取二进制文件"></a>获取二进制文件</h2><p><em>所有操作都在master结点上执行</em></p>
<p><strong>获取安装文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wget https://dl.k8s.io/v1.15.5/kubernetes-server-linux-amd64.tar.gz</span><br><span class="line">tar xf kubernetes-server-linux-amd64.tar.gz</span><br><span class="line">cd kubernetes/server/bin</span><br><span class="line">cp kube-apiserver kube-controller-manager kubectl /usr/bin</span><br><span class="line">cp kube-scheduler kubelet kube-proxy  /usr/bin</span><br></pre></td></tr></table></figure>


<span id="more"></span>


<h2 id="TLS-Bootstrapping-Token"><a href="#TLS-Bootstrapping-Token" class="headerlink" title="TLS Bootstrapping Token"></a>TLS Bootstrapping Token</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># head -c 16 /dev/urandom | od -An -t x | tr -d &#x27; &#x27;</span><br><span class="line">449bbeb0ea7e50f321087a123a509a19</span><br><span class="line"></span><br><span class="line"># vim /cloud/k8s/kubernetes/cfg/token.csv</span><br><span class="line">449bbeb0ea7e50f321087a123a509a19,kubelet-bootstrap,10001,&quot;system:kubelet-bootstrap&quot;</span><br></pre></td></tr></table></figure>


<h2 id="kubernetes机器提供HTTPS服务"><a href="#kubernetes机器提供HTTPS服务" class="headerlink" title="kubernetes机器提供HTTPS服务"></a>kubernetes机器提供HTTPS服务</h2><p><strong>安装配置CFSSL</strong></p>
<p><em>创建证书工具,用于提供https服务</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wget https://pkg.cfssl.org/R1.2/cfssl_linux-amd64</span><br><span class="line">wget https://pkg.cfssl.org/R1.2/cfssljson_linux-amd64</span><br><span class="line">wget https://pkg.cfssl.org/R1.2/cfssl-certinfo_linux-amd64</span><br><span class="line">chmod +x cfssl_linux-amd64 cfssljson_linux-amd64 cfssl-certinfo_linux-amd64</span><br><span class="line">mv cfssl_linux-amd64 /usr/local/bin/cfssl</span><br><span class="line">mv cfssljson_linux-amd64 /usr/local/bin/cfssljson</span><br><span class="line">mv cfssl-certinfo_linux-amd64 /usr/bin/cfssl-certinfo</span><br></pre></td></tr></table></figure>


<p><strong>cs根证书</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; EOF | tee /etc/kubernetes/ssl/ca-config.json</span><br><span class="line">&#123;</span><br><span class="line">  &quot;signing&quot;: &#123;</span><br><span class="line">    &quot;default&quot;: &#123;</span><br><span class="line">      &quot;expiry&quot;: &quot;87600h&quot;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;profiles&quot;: &#123;</span><br><span class="line">      &quot;kubernetes&quot;: &#123;</span><br><span class="line">         &quot;expiry&quot;: &quot;87600h&quot;,</span><br><span class="line">         &quot;usages&quot;: [</span><br><span class="line">            &quot;signing&quot;,</span><br><span class="line">            &quot;key encipherment&quot;,</span><br><span class="line">            &quot;server auth&quot;,</span><br><span class="line">            &quot;client auth&quot;</span><br><span class="line">        ]</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; EOF | tee /etc/kubernetes/ssl/ca-csr.json</span><br><span class="line">&#123;</span><br><span class="line">    &quot;CN&quot;: &quot;kubernetes&quot;,</span><br><span class="line">    &quot;key&quot;: &#123;</span><br><span class="line">        &quot;algo&quot;: &quot;rsa&quot;,</span><br><span class="line">        &quot;size&quot;: 2048</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;names&quot;: [</span><br><span class="line">        &#123;</span><br><span class="line">            &quot;C&quot;: &quot;CN&quot;,</span><br><span class="line">            &quot;L&quot;: &quot;Shanghai&quot;,</span><br><span class="line">            &quot;ST&quot;: &quot;Shanghai&quot;,</span><br><span class="line">            &quot;O&quot;: &quot;k8s&quot;,</span><br><span class="line">            &quot;OU&quot;: &quot;System&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 生成根证书</span><br><span class="line">cd /etc/kubernetes/ssl</span><br><span class="line">cfssl gencert -initca /etc/kubernetes/ssl/ca-csr.json | cfssljson -bare ca -</span><br></pre></td></tr></table></figure>

<p><strong>kube-apiserver证书</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># master结点主机名</span><br><span class="line">cat &lt;&lt; EOF | tee /etc/kubernetes/ssl/server-csr.json</span><br><span class="line">&#123;</span><br><span class="line">    &quot;CN&quot;: &quot;kubernetes&quot;,</span><br><span class="line">    &quot;hosts&quot;: [</span><br><span class="line">        &quot;127.0.0.1&quot;,</span><br><span class="line">        &quot;$&#123;master_ip&#125;&quot;,</span><br><span class="line">        &quot;10.40.0.1&quot;,</span><br><span class="line">        &quot;kubernetes&quot;,</span><br><span class="line">        &quot;kubernetes.default&quot;,</span><br><span class="line">        &quot;kubernetes.default.svc&quot;,</span><br><span class="line">        &quot;kubernetes.default.svc.cluster&quot;,</span><br><span class="line">        &quot;kubernetes.default.svc.cluster.local&quot;</span><br><span class="line">    ],</span><br><span class="line">    &quot;key&quot;: &#123;</span><br><span class="line">        &quot;algo&quot;: &quot;rsa&quot;,</span><br><span class="line">        &quot;size&quot;: 2048</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;names&quot;: [</span><br><span class="line">        &#123;</span><br><span class="line">            &quot;C&quot;: &quot;CN&quot;,</span><br><span class="line">            &quot;L&quot;: &quot;Shanghai&quot;,</span><br><span class="line">            &quot;ST&quot;: &quot;Shanghai&quot;,</span><br><span class="line">            &quot;O&quot;: &quot;k8s&quot;,</span><br><span class="line">            &quot;OU&quot;: &quot;System&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 生成apiserver的证书</span><br><span class="line">cd /etc/kubernetes/ssl/</span><br><span class="line">cfssl gencert -ca=/etc/kubernetes/ssl/ca.pem -ca-key=/etc/kubernetes/ssl/ca-key.pem -config=/etc/kubernetes/ssl/ca-config.json -profile=kubernetes \</span><br><span class="line">    /etc/kubernetes/ssl/server-csr.json | cfssljson -bare server</span><br></pre></td></tr></table></figure>


<p><strong>kube-proxy证书</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; EOF | tee /etc/kubernetes/ssl/kube-proxy-csr.json</span><br><span class="line">&#123;</span><br><span class="line">  &quot;CN&quot;: &quot;system:kube-proxy&quot;,</span><br><span class="line">  &quot;hosts&quot;: [],</span><br><span class="line">  &quot;key&quot;: &#123;</span><br><span class="line">    &quot;algo&quot;: &quot;rsa&quot;,</span><br><span class="line">    &quot;size&quot;: 2048</span><br><span class="line">  &#125;,</span><br><span class="line">  &quot;names&quot;: [</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;C&quot;: &quot;CN&quot;,</span><br><span class="line">      &quot;L&quot;: &quot;Shanghai&quot;,</span><br><span class="line">      &quot;ST&quot;: &quot;Shanghai&quot;,</span><br><span class="line">      &quot;O&quot;: &quot;k8s&quot;,</span><br><span class="line">      &quot;OU&quot;: &quot;System&quot;</span><br><span class="line">    &#125;</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 生成kube-proxy证书</span><br><span class="line">cd /etc/kubernetes/ssl/</span><br><span class="line">cfssl gencert -ca=/etc/kubernetes/ssl/ca.pem \</span><br><span class="line">-ca-key=/etc/kubernetes/ssl/ca-key.pem \</span><br><span class="line">-config=/etc/kubernetes/ssl/ca-config.json \</span><br><span class="line">-profile=kubernetes /etc/kubernetes/ssl/kube-proxy-csr.json \</span><br><span class="line">| cfssljson -bare kube-proxy</span><br></pre></td></tr></table></figure>


<h2 id="kube-apiserver服务"><a href="#kube-apiserver服务" class="headerlink" title="kube-apiserver服务"></a>kube-apiserver服务</h2><p><strong>配置文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># vim /etc/kubernetes/kube-apiserver.conf</span><br><span class="line">KUBE_APISERVER_OPTS=&quot;--logtostderr=false \</span><br><span class="line">--log-dir=/var/log/kubernetes \</span><br><span class="line">--v=4 \</span><br><span class="line">--etcd-servers=http://$&#123;etcd_cluster_ip&#125;:2379 \</span><br><span class="line">--bind-address=$&#123;master_ip&#125; \</span><br><span class="line">--secure-port=6443 \</span><br><span class="line">--advertise-address=$&#123;master_ip&#125; \</span><br><span class="line">--allow-privileged=true \</span><br><span class="line">--service-cluster-ip-range=10.40.0.0/24 \</span><br><span class="line">--enable-admission-plugins=NamespaceLifecycle,LimitRanger,ServiceAccount,ResourceQuota,NodeRestriction \</span><br><span class="line">--authorization-mode=RBAC,Node \</span><br><span class="line">--enable-bootstrap-token-auth \</span><br><span class="line">--token-auth-file=/etc/kubernetes/token.csv \</span><br><span class="line">--service-node-port-range=30000-50000 \</span><br><span class="line">--tls-cert-file=/etc/kubernetes/ssl/server.pem  \</span><br><span class="line">--tls-private-key-file=/etc/kubernetes/ssl/server-key.pem \</span><br><span class="line">--client-ca-file=/etc/kubernetes/ssl/ca.pem \</span><br><span class="line">--service-account-key-file=/etc/kubernetes/ssl/ca-key.pem&quot;</span><br></pre></td></tr></table></figure>

<p><strong>systemctl service文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; \EOF &gt; /lib/systemd/system/kube-apiserver.service</span><br><span class="line">[Unit]</span><br><span class="line">Description=Kubernetes API Server</span><br><span class="line">Documentation=https://github.com/kubernetes/kubernetes</span><br><span class="line">After=flannld.service</span><br><span class="line">[Service]</span><br><span class="line">EnvironmentFile=/etc/kubernetes/kube-apiserver.conf</span><br><span class="line">ExecStart=/usr/bin/kube-apiserver $KUBE_APISERVER_OPTS</span><br><span class="line">Restart=on-failure</span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>


<p><strong>启动kube-apiserver服务</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br><span class="line">systemctl enable kube-apiserver</span><br><span class="line">systemctl start kube-apiserver</span><br></pre></td></tr></table></figure>


<h2 id="kube-controller-manager服务"><a href="#kube-controller-manager服务" class="headerlink" title="kube-controller-manager服务"></a>kube-controller-manager服务</h2><p><strong>配置文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># vim /etc/kubernetes/kube-controller-manager.conf</span><br><span class="line">KUBE_CONTROLLER_MANAGER_OPTS=&quot;--logtostderr=false \</span><br><span class="line">--log-dir=/var/log/kubernetes \</span><br><span class="line">--v=4 \</span><br><span class="line">--master=127.0.0.1:8080 \</span><br><span class="line">--leader-elect=true \</span><br><span class="line">--address=127.0.0.1 \</span><br><span class="line">--service-cluster-ip-range=10.40.0.0/24 \</span><br><span class="line">--cluster-name=kubernetes \</span><br><span class="line">--cluster-signing-cert-file=/etc/kubernetes/ssl/ca.pem \</span><br><span class="line">--cluster-signing-key-file=/etc/kubernetes/ssl/ca-key.pem  \</span><br><span class="line">--root-ca-file=/etc/kubernetes/ssl/ca.pem \</span><br><span class="line">--service-account-private-key-file=/etc/kubernetes/ssl/ca-key.pem&quot;</span><br></pre></td></tr></table></figure>

<p><strong>systemctl service文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; \EOF &gt; /lib/systemd/system/kube-controller-manager.service</span><br><span class="line">[Unit]</span><br><span class="line">Description=Kubernetes Controller Manager</span><br><span class="line">Documentation=https://github.com/kubernetes/kubernetes</span><br><span class="line">After=kube-apiserver.service</span><br><span class="line">[Service]</span><br><span class="line">EnvironmentFile=-/etc/kubernetes/kube-controller-manager.conf</span><br><span class="line">ExecStart=/usr/bin/kube-controller-manager $KUBE_CONTROLLER_MANAGER_OPTS</span><br><span class="line">Restart=on-failure</span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<p><strong>启动kube-controller-manager服务</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br><span class="line">systemctl enable kube-controller-manager</span><br><span class="line">systemctl start kube-controller-manager</span><br></pre></td></tr></table></figure>


<h2 id="kube-scheduler服务"><a href="#kube-scheduler服务" class="headerlink" title="kube-scheduler服务"></a>kube-scheduler服务</h2><p><strong>配置文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># vim /etc/kubernetes/kube-scheduler.conf</span><br><span class="line">KUBE_SCHEDULER_OPTS=&quot;--logtostderr=false --log-dir=/var/log/kubernetes \</span><br><span class="line"> --v=4 --master=127.0.0.1:8080 --leader-elect&quot;</span><br></pre></td></tr></table></figure>

<p><strong>systemctl service文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; \EOF &gt; /lib/systemd/system/kube-scheduler.service</span><br><span class="line">[Unit]</span><br><span class="line">Description=Kubernetes Scheduler</span><br><span class="line">Documentation=https://github.com/kubernetes/kubernetes</span><br><span class="line">After=kube-apiserver.service</span><br><span class="line">[Service]</span><br><span class="line">EnvironmentFile=-/etc/kubernetes/kube-scheduler.conf</span><br><span class="line">ExecStart=/usr/bin/kube-scheduler $KUBE_SCHEDULER_OPTS</span><br><span class="line">Restart=on-failure</span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<p><strong>启动kube-scheduler服务</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br><span class="line">systemctl enable kube-scheduler</span><br><span class="line">systemctl start kube-scheduler</span><br></pre></td></tr></table></figure>



<h2 id="创建-kubelet-bootstrap-kubeconfig-文件"><a href="#创建-kubelet-bootstrap-kubeconfig-文件" class="headerlink" title="创建 kubelet bootstrap kubeconfig 文件"></a>创建 kubelet bootstrap kubeconfig 文件</h2><p><strong>kubeconfig</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">BOOTSTRAP_TOKEN=449bbeb0ea7e50f321087a123a509a19</span><br><span class="line">KUBE_APISERVER=&quot;https://$&#123;master_ip&#125;:6443&quot;</span><br><span class="line"># 设置集群参数</span><br><span class="line">kubectl config set-cluster kubernetes \</span><br><span class="line">  --certificate-authority=/etc/kubernetes/ssl/ca.pem \</span><br><span class="line">  --embed-certs=true \</span><br><span class="line">  --server=$&#123;KUBE_APISERVER&#125; \</span><br><span class="line">  --kubeconfig=bootstrap.kubeconfig</span><br><span class="line"># 设置客户端认证参数</span><br><span class="line">kubectl config set-credentials kubelet-bootstrap \</span><br><span class="line">  --token=$&#123;BOOTSTRAP_TOKEN&#125; \</span><br><span class="line">  --kubeconfig=bootstrap.kubeconfig</span><br><span class="line"># 设置上下文参数</span><br><span class="line">kubectl config set-context default \</span><br><span class="line">  --cluster=kubernetes \</span><br><span class="line">  --user=kubelet-bootstrap \</span><br><span class="line">  --kubeconfig=bootstrap.kubeconfig</span><br><span class="line"># 设置默认上下文</span><br><span class="line">kubectl config use-context default --kubeconfig=bootstrap.kubeconfig</span><br><span class="line"></span><br><span class="line"># 创建kube-proxy kubeconfig文件</span><br><span class="line">kubectl config set-cluster kubernetes \</span><br><span class="line">  --certificate-authority=/etc/kubernetes/ssl/ca.pem \</span><br><span class="line">  --embed-certs=true \</span><br><span class="line">  --server=$&#123;KUBE_APISERVER&#125; \</span><br><span class="line">  --kubeconfig=kube-proxy.kubeconfig</span><br><span class="line">kubectl config set-credentials kube-proxy \</span><br><span class="line">  --client-certificate=/etc/kubernetes/ssl/kube-proxy.pem \</span><br><span class="line">  --client-key=/etc/kubernetes/ssl/kube-proxy-key.pem \</span><br><span class="line">  --embed-certs=true \</span><br><span class="line">  --kubeconfig=kube-proxy.kubeconfig</span><br><span class="line">kubectl config set-context default \</span><br><span class="line">  --cluster=kubernetes \</span><br><span class="line">  --user=kube-proxy \</span><br><span class="line">  --kubeconfig=kube-proxy.kubeconfig</span><br><span class="line">kubectl config use-context default --kubeconfig=kube-proxy.kubeconfig</span><br></pre></td></tr></table></figure>


<h2 id="kubelet服务"><a href="#kubelet服务" class="headerlink" title="kubelet服务"></a>kubelet服务</h2><p><strong>配置文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># node-labels=node-role.kubernetes.io/master, master结点使用该参数</span><br><span class="line"># node-labels=node-role.kubernetes.io/node, node结点使用该参数</span><br><span class="line"># vim /etc/kubernetes/kubelet.conf</span><br><span class="line">KUBELET_OPTS=&quot;--logtostderr=false \</span><br><span class="line">--log-dir=/var/log/kubernetes \</span><br><span class="line">--v=4 \</span><br><span class="line">--node-labels=node-role.kubernetes.io/master \</span><br><span class="line">--hostname-override=k8s-master \</span><br><span class="line">--kubeconfig=/etc/kubernetes/kubelet.kubeconfig \</span><br><span class="line">--bootstrap-kubeconfig=/etc/kubernetes/bootstrap.kubeconfig \</span><br><span class="line">--config=/etc/kubernetes/kubelet.config \</span><br><span class="line">--cert-dir=/etc/kubernetes/ssl \</span><br><span class="line">--pod-infra-container-image=registry.cn-hangzhou.aliyuncs.com/google-containers/pause-amd64:3.0&quot;</span><br></pre></td></tr></table></figure>


<p><strong>kubelet配置模板</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># vim /etc/kubernetes/kubelet.config</span><br><span class="line">kind: KubeletConfiguration</span><br><span class="line">apiVersion: kubelet.config.k8s.io/v1beta1</span><br><span class="line">address: $&#123;node_ip&#125;</span><br><span class="line">port: 10250</span><br><span class="line">readOnlyPort: 10255</span><br><span class="line">cgroupDriver: systemd</span><br><span class="line">clusterDomain: cluster.local.</span><br><span class="line">failSwapOn: false</span><br><span class="line">authentication:</span><br><span class="line">  anonymous:</span><br><span class="line">    enabled: true</span><br></pre></td></tr></table></figure>

<p><strong>systemctl service文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; \EOF &gt; /lib/systemd/system/kubelet.service</span><br><span class="line">[Unit]</span><br><span class="line">Description=Kubernetes Kubelet</span><br><span class="line">After=docker.service</span><br><span class="line">Requires=docker.service</span><br><span class="line">[Service]</span><br><span class="line">EnvironmentFile=/etc/kubernetes/kubelet.conf</span><br><span class="line">ExecStart=/usr/bin/kubelet $KUBELET_OPTS</span><br><span class="line">Restart=on-failure</span><br><span class="line">KillMode=process</span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<p><strong>启动kubelet服务</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br><span class="line">systemctl enable kubelet</span><br><span class="line">systemctl start kubelet</span><br></pre></td></tr></table></figure>


<h2 id="kube-proxy服务"><a href="#kube-proxy服务" class="headerlink" title="kube-proxy服务"></a>kube-proxy服务</h2><p><strong>配置文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># vim /etc/kubernetes/kube-proxy.conf</span><br><span class="line">KUBE_PROXY_OPTS=&quot;--logtostderr=true \</span><br><span class="line">--v=4 \</span><br><span class="line">--hostname-override=k8s-master \</span><br><span class="line">--cluster-cidr=10.40.0.0/24 \</span><br><span class="line">--kubeconfig=/etc/kubernetes/kube-proxy.kubeconfig&quot;</span><br></pre></td></tr></table></figure>

<p><strong>systemctl service文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; \EOF &gt; /lib/systemd/system/kube-proxy.service</span><br><span class="line">[Unit]</span><br><span class="line">Description=Kubernetes Proxy</span><br><span class="line">After=network.target</span><br><span class="line">[Service]</span><br><span class="line">EnvironmentFile=-/etc/kubernetes/kube-proxy.conf</span><br><span class="line">ExecStart=/usr/bin/kube-proxy $KUBE_PROXY_OPTS</span><br><span class="line">Restart=on-failure</span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<p><strong>启动kube-proxy服务</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br><span class="line">systemctl enable kube-proxy</span><br><span class="line">systemctl start kube-proxy</span><br></pre></td></tr></table></figure>


<h2 id="绑定kubelet-bootstrap用户到集群"><a href="#绑定kubelet-bootstrap用户到集群" class="headerlink" title="绑定kubelet-bootstrap用户到集群"></a>绑定kubelet-bootstrap用户到集群</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">kubectl create clusterrolebinding kubelet-bootstrap \</span><br><span class="line">  --clusterrole=system:node-bootstrapper \</span><br><span class="line">  --user=kubelet-bootstrap</span><br></pre></td></tr></table></figure>


<h2 id="CoreDns"><a href="#CoreDns" class="headerlink" title="CoreDns"></a>CoreDns</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/coredns/deployment.git</span><br><span class="line">cd kubernetes</span><br><span class="line">yum -y install jq</span><br><span class="line">./deploy.sh -r 10.40.0.0/24 -i 10.40.0.5 -d cluster.local |kubectl apply -f -</span><br></pre></td></tr></table></figure>


<h2 id="kubernetes-node部署"><a href="#kubernetes-node部署" class="headerlink" title="kubernetes node部署"></a>kubernetes node部署</h2><p><em>更新kube-proxy、kubelet配置文件, hostname-override是当前机器主机名</em></p>
<p><em>kubelet配置文件: node-labels&#x3D;node-role.kubernetes.io&#x2F;master 更新为node-labels&#x3D;node-role.kubernetes.io&#x2F;node</em></p>
<p><em>更新docker service文件,从flannel获取网桥信息</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scp /etc/kubernetes/ssl/* root@node:/etc/kubernetes/ssl</span><br><span class="line">scp /etc/kubernetes/*.kubeconfig root@node:/etc/kubernetes</span><br><span class="line">scp /etc/kubernetes/kube-proxy.conf root@node:/etc/kubernetes</span><br><span class="line">scp /etc/kubernetes/kubelet.conf root@node:/etc/kubernetes</span><br><span class="line">scp /usr/bin/kube-proxy root@node:/usr/bin</span><br><span class="line">scp /usr/bin/kubelet root@node:/usr/bin</span><br><span class="line">scp /lib/systemd/system/kubelet.service root@node:/lib/systemd/system</span><br><span class="line">scp /lib/systemd/system/kube-proxy.service root@node:/lib/systemd/system</span><br><span class="line"></span><br><span class="line">mkdir /etc/flannel</span><br><span class="line">scp /usr/bin/flanneld /usr/bin/mk-docker-opts.sh root@node:/usr/bin/</span><br><span class="line">scp /lib/systemd/system/flanneld.service root@node:/lib/systemd/system/</span><br><span class="line">scp /etc/flannel/flannel.conf root@node:/etc/flannel/</span><br></pre></td></tr></table></figure>

<p><strong>启动服务</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br><span class="line">mkdir /var/lib/kubelet</span><br><span class="line">systemctl enable kubelet</span><br><span class="line">systemctl start kubelet</span><br><span class="line">systemctl enable kube-proxy</span><br><span class="line">systemctl start kube-proxy</span><br></pre></td></tr></table></figure>

<h2 id="node结点加入集群"><a href="#node结点加入集群" class="headerlink" title="node结点加入集群"></a>node结点加入集群</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">kubectl get csr</span><br><span class="line"># 手动接收认证</span><br><span class="line">kubectl certificate approve $NAME</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s service底层之iptables与ipvs</title>
    <url>//2023/05/08/k8s/service%E5%BA%95%E5%B1%82%E4%B9%8Biptables%E4%B8%8Eipvs/</url>
    <content><![CDATA[<p><em><strong>service底层实现主要有两个网络模式: iptables和ipvs. 他们都是由kube-proxy维护</strong></em></p>
<h2 id="Iptables"><a href="#Iptables" class="headerlink" title="Iptables"></a>Iptables</h2><p><code>kube-proxy</code>通过Service的Informer感知到ApiServer的<code>Service</code>和<code>EndPoint</code>的变化情况。作为该事件的响应，它会在宿主机上创建相应的<code>iptables</code>规则。这些规则会捕获到<code>Service</code>的ClusterIp和Port的流量，并将流量按照比例重定向到<code>Service</code>的后端<code>Pod</code>中</p>
<span id="more"></span>

<p><code>kube-proxy</code>在iptables的NAT表PREROUTING链中实现它的NAT和负载均衡功能</p>
<p><code>Iptables</code>为防火墙而设计且基于内核规则列表，kube-proxy 使用的是一种 O(n) 算法，其中的 n 随集群规模同步增长，所以这里的集群规模越大，更明确的说就是服务和后端<code>Pod</code>的数量越大，查询的时间就会越长</p>
<p><font color='red'><strong>Tips</strong></font>: 当后端<code>Pod</code>不可用时无法进行重试</p>
<p><img src="/images/k8s_service_iptables.png"></p>
<h2 id="Ipvs"><a href="#Ipvs" class="headerlink" title="Ipvs"></a>Ipvs</h2><p>基于IPVS实现Service转发，Kubernetes几乎能够具备无限的水平扩展能力</p>
<p>在IPVS模式下，<code>kube-proxy</code>通过Service的Informer感知到ApiServer的<code>Service</code>和<code>Endpoint</code>的变化情况。作为该事件的响应，它会调用netlink接口创建IPVS规则，并定期将IPVS规则与K8S的<code>Service</code>和<code>Endpoint</code>同步。访问服务时，IPVS 将流量定向到后端Pod之一。</p>
<p>当我们创建了前面的<code>Service</code>之后，kube-proxy首先会在宿主机上创建一个虚拟网卡（叫作：kube-ipvs0），并为它分配Service VIP作为IP地址。接下来，kube-proxy就会通过Linux的IPVS模块，为这个IP地址设置三个 IPVS虚拟主机，并设置这三个虚拟主机之间使用轮询模式 (rr) 来作为负载均衡策略。拓扑图如下所示拓扑图：</p>
<p><img src="/images/k8s_service_ipvs.png"></p>
]]></content>
      <tags>
        <tag>linux</tag>
        <tag>iptables</tag>
        <tag>kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>基于openssl的kubernetes证书</title>
    <url>//2020/02/11/k8s/%E5%9F%BA%E4%BA%8Eopenssl%E7%9A%84kubernetes%E8%AF%81%E4%B9%A6/</url>
    <content><![CDATA[<h1 id="kube-apiserver的CA证书"><a href="#kube-apiserver的CA证书" class="headerlink" title="kube-apiserver的CA证书"></a>kube-apiserver的CA证书</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out ca.key 2048</span><br><span class="line">openssl req -x509 -new -nodes -key ca.key -subj &quot;/CN=&lt;master_hostname&gt;&quot; \</span><br><span class="line">    -days 5000 -out ca.crt</span><br><span class="line">openssl genrsa -out server.key 2048</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<p><em>master_ssl.cnf</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[req]</span><br><span class="line">req_extensions = v3_req</span><br><span class="line">distinguished_name = req_distinguished_name</span><br><span class="line">[req_distinguished_name]</span><br><span class="line">[ v3_req ]</span><br><span class="line">basicConstraints = CA:FALSE</span><br><span class="line">keyUsage = nonRepudiation, digitalSignature, keyEncipherment</span><br><span class="line">subjectAltName = @alt_names</span><br><span class="line">[alt_names]</span><br><span class="line">DNS.1 = kubernetes</span><br><span class="line">DNS.2 = kubernetes.default</span><br><span class="line">DNS.3 = kubernetes.default.svc</span><br><span class="line">DNS.4 = kubernetes.default.svc.cluster.local</span><br><span class="line">DNS.5 = &lt;master_hostname&gt;</span><br><span class="line">IP.1 = &lt;master_ip&gt;</span><br><span class="line">IP.3 = &lt;kubectl get svc | grep kubernetes | awk &#x27;&#123;print $3&#125;&#x27;&gt;集群IP</span><br></pre></td></tr></table></figure>

<p><strong>基于master_ssl.conf创建server.csr和server.crt文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">openssl req -new -key server.key -subj &quot;/CN=&lt;master_hostname&gt;&quot; -config \</span><br><span class="line">master_ssl.cnf -out server.csr</span><br><span class="line">openssl x509 -req -in server.csr -CA ca.crt -CAkey ca.key -CAcreateserial \</span><br><span class="line">-days 5000 -extensions v3_req -extfile master_ssl.cnf -out server.crt</span><br></pre></td></tr></table></figure>


<p><strong>kube-apiserver启动参数</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--client-ca-file=&lt;ca_dir&gt;/ca.crt</span><br><span class="line">--tls-private-key-file=&lt;ca_dir&gt;/server.key</span><br><span class="line">--tls-cert-file=&lt;ca_dir&gt;/server.crt</span><br><span class="line">--insecure-port=0</span><br><span class="line">--secure-port=6443</span><br></pre></td></tr></table></figure>


<h1 id="kube-controller-manager证书"><a href="#kube-controller-manager证书" class="headerlink" title="kube-controller-manager证书"></a>kube-controller-manager证书</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out cs_client.key 2048</span><br><span class="line">openssl req -new -key ca.key -subj &quot;/CN=&lt;master_hostname&gt;&quot; -out cs_client.csr</span><br><span class="line">openssl x509 -req -in cs_client.csr -CA ca.crt -CAkey ca.key -CAcreateserial \</span><br><span class="line">-out cs_client.crt -days 5000</span><br></pre></td></tr></table></figure>

<p><em>controller-manager.kubeconfig</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">apiVersion: v1</span><br><span class="line">kind: Config</span><br><span class="line">users:</span><br><span class="line">- name: controllermanager</span><br><span class="line">  user:</span><br><span class="line">    client-certificate: &lt;ca_dir&gt;/cs_client.crt</span><br><span class="line">    client-key: &lt;ca_dir&gt;/cs_client.key</span><br><span class="line">clusters:</span><br><span class="line">- name: local</span><br><span class="line">  cluster:</span><br><span class="line">    certificate-authority: &lt;ca_dir&gt;/ca.crt</span><br><span class="line">    server: https://&lt;apiserver_ip&gt;:6443</span><br><span class="line">contexts:</span><br><span class="line">- context:</span><br><span class="line">    cluster: local</span><br><span class="line">    user: controllermanager</span><br><span class="line">  name: my-context</span><br><span class="line">current-context: my-context</span><br></pre></td></tr></table></figure>

<p><em>kube-controller-manger服务的启动参数</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--service-account-key-file=&lt;ca_dir&gt;/server.key</span><br><span class="line">--root-ca-file=&lt;ca_dir&gt;/ca.crt</span><br><span class="line">--kubeconfig=controller-manager.kubeconfig</span><br></pre></td></tr></table></figure>

<h1 id="kube-scheduler"><a href="#kube-scheduler" class="headerlink" title="kube-scheduler"></a>kube-scheduler</h1><p><em>启动参数</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--kubeconfig=controller-manager.kubeconfig</span><br></pre></td></tr></table></figure>


<h1 id="kubelet证书"><a href="#kubelet证书" class="headerlink" title="kubelet证书"></a>kubelet证书</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out kubelet_client.key 2048</span><br><span class="line">openssl req -new -key kubelet_client.key -subj &quot;/CN=&lt;node_hostname&gt;&quot; -out \</span><br><span class="line">kubele_client.csr</span><br><span class="line">openssl x509 -req -in kubelet_client.csr -CA ca.crt -CAkey ca.key \</span><br><span class="line">-CAcreateserial -out kubelet_client.cet -days 5000</span><br></pre></td></tr></table></figure>

<p><em>kubelet.kubeconfig</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">apiVersion: v1</span><br><span class="line">kind: Config</span><br><span class="line">users:</span><br><span class="line">- name: kubelet</span><br><span class="line">  user: </span><br><span class="line">    client-certificate: &lt;ca_dir&gt;/kubelet_client.crt</span><br><span class="line">    client-key: &lt;ca_dir&gt;/kubelet_client.key</span><br><span class="line">clusters:</span><br><span class="line">- name: local</span><br><span class="line">  cluster:</span><br><span class="line">    certificate-authority: &lt;ca_dir&gt;/ca.crt</span><br><span class="line">    server: https://&lt;api_server&gt;:6443</span><br><span class="line">contexts:</span><br><span class="line">- context:</span><br><span class="line">    cluster: local</span><br><span class="line">    user: kubelet</span><br><span class="line">  name: my-context</span><br><span class="line">current-context: my-context</span><br></pre></td></tr></table></figure>

<p><strong>kubelet服务启动参数</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--kubeconfig=kubelet.kubeconfig</span><br></pre></td></tr></table></figure>

<h1 id="kube-proxy"><a href="#kube-proxy" class="headerlink" title="kube-proxy"></a>kube-proxy</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--kubeconfig=kubelet.kubeconfig</span><br></pre></td></tr></table></figure>


<h1 id="kubectl"><a href="#kubectl" class="headerlink" title="kubectl"></a>kubectl</h1><p><em>安全访问apiserver</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">kubectl --server=https://&lt;apiserver&gt;:6443 \</span><br><span class="line">--certificate-authority=&lt;ca_dir&gt;/ca.crt \</span><br><span class="line">--client-certificate=&lt;ca_dir&gt;/cs_client.crt \</span><br><span class="line">--clinet-key=&lt;ca_dir&gt;/cs_client.key</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>kubernetes</tag>
        <tag>openssl</tag>
      </tags>
  </entry>
  <entry>
    <title>kubernetes集群部署(二):etcd集群</title>
    <url>//2020/02/08/k8s/kubernetes%E9%9B%86%E7%BE%A4%E9%83%A8%E7%BD%B2(%E4%BA%8C)_etcd%E9%9B%86%E7%BE%A4/</url>
    <content><![CDATA[<h2 id="安装etcd服务"><a href="#安装etcd服务" class="headerlink" title="安装etcd服务"></a>安装etcd服务</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wget https://github.com/etcd-io/etcd/releases/download/ \</span><br><span class="line">    v3.3.17/etcd-v3.3.18-linux-arm64.tar.gz</span><br><span class="line">tar xf etcd-v3.3.17-linux-amd64.tar.gz</span><br><span class="line">cd etcd-v3.3.17-linux-amd64</span><br><span class="line">mv etcd* /usr/bin/</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<br/>
<br/>

<h2 id="单节点部署"><a href="#单节点部署" class="headerlink" title="单节点部署"></a>单节点部署</h2><h6 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># etho是网卡名称</span><br><span class="line">node_ip=$(ifconfig etho | grep inet | grep -v inet6 | awk &#x27;&#123;print $2&#125;&#x27;)</span><br><span class="line">cat &lt;&lt; EOF | tee /etc/etcd/etcd.conf</span><br><span class="line">ETCD_NAME=&quot;etcd01&quot;</span><br><span class="line">ETCD_DATA_DIR=&quot;/var/lib/etcd&quot;</span><br><span class="line">ETCD_LISTEN_PEER_URLS=&quot;http://$&#123;node_ip&#125;:2380&quot;</span><br><span class="line">ETCD_LISTEN_CLIENT_URLS=&quot;http://$&#123;node_ip&#125;:2379&quot;</span><br><span class="line">ETCD_ADVERTISE_CLIENT_URLS=&quot;http://$&#123;node_ip&#125;:2379&quot;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>


<h6 id="systemctl-service文件"><a href="#systemctl-service文件" class="headerlink" title="systemctl service文件"></a>systemctl service文件</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; \EOF | tee /lib/systemd/system/etcd.service</span><br><span class="line">[Unit]</span><br><span class="line">Description=Etcd Server</span><br><span class="line">After=network.target</span><br><span class="line">After=network-online.target</span><br><span class="line">Wants=network-online.target</span><br><span class="line">[Service]</span><br><span class="line">Type=notify</span><br><span class="line">EnvironmentFile=/etc/etcd/etcd.conf</span><br><span class="line">ExecStart=/usr/bin/etcd \</span><br><span class="line">--name=$&#123;ETCD_NAME&#125; \</span><br><span class="line">--data-dir=$&#123;ETCD_DATA_DIR&#125; \</span><br><span class="line">--listen-peer-urls=$&#123;ETCD_LISTEN_PEER_URLS&#125; \</span><br><span class="line">--listen-client-urls=$&#123;ETCD_LISTEN_CLIENT_URLS&#125;,http://127.0.0.1:2379 \</span><br><span class="line">--advertise-client-urls=$&#123;ETCD_ADVERTISE_CLIENT_URLS&#125;</span><br><span class="line">Restart=on-failure</span><br><span class="line">LimitNOFILE=65536</span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<br/>
<br/>

<h2 id="etcd集群"><a href="#etcd集群" class="headerlink" title="etcd集群"></a>etcd集群</h2><h6 id="配置文件-1"><a href="#配置文件-1" class="headerlink" title="配置文件"></a>配置文件</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># master结点的IP地址</span><br><span class="line">master_ip=&quot;10.0.2.15&quot;</span><br><span class="line"># etho是网卡名称, node_ip是etcd结点的IP地址</span><br><span class="line">node_ip=$(ifconfig etho | grep inet | grep -v inet6 | awk &#x27;&#123;print $2&#125;&#x27;)</span><br><span class="line"></span><br><span class="line">cat &lt;&lt; EOF | tee /etc/etcd/etcd.conf</span><br><span class="line">ETCD_NAME=&quot;etcd01&quot;　# &#123;etcd01, etcd02, etcd03&#125;, 结点名称</span><br><span class="line">ETCD_DATA_DIR=&quot;/var/lib/etcd&quot;</span><br><span class="line">ETCD_LISTEN_PEER_URLS=&quot;http://$&#123;node_ip&#125;:2380&quot;</span><br><span class="line">ETCD_LISTEN_CLIENT_URLS=&quot;http://$&#123;node_ip&#125;:2379&quot;</span><br><span class="line">ETCD_ADVERTISE_CLIENT_URLS=&quot;http://$&#123;node_ip&#125;:2379&quot;</span><br><span class="line">ETCD_INITIAL_ADVERTISE_PEER_URLS=&quot;https://$&#123;node_ip&#125;:2380&quot;</span><br><span class="line"># node&#123;num&#125;_ip是每个结点的IP地址</span><br><span class="line">ETCD_INITIAL_CLUSTER=&quot;etcd01=https://$&#123;master_ip&#125;:2380,\</span><br><span class="line">    etcd02=https://$&#123;node1_ip&#125;:2380,etcd03=https://$&#123;node2_ip&#125;:2380&quot;</span><br><span class="line">ETCD_INITIAL_CLUSTER_TOKEN=&quot;etcd-cluster&quot;</span><br><span class="line">ETCD_INITIAL_CLUSTER_STATE=&quot;new&quot;</span><br></pre></td></tr></table></figure>

<h6 id="systemctl-service文件-1"><a href="#systemctl-service文件-1" class="headerlink" title="systemctl service文件"></a>systemctl service文件</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; \EOF | tee /lib/systemd/system/etcd.service</span><br><span class="line">[Unit]</span><br><span class="line">Description=Etcd Server</span><br><span class="line">After=network.target</span><br><span class="line">After=network-online.target</span><br><span class="line">Wants=network-online.target</span><br><span class="line">[Service]</span><br><span class="line">Type=notify</span><br><span class="line">EnvironmentFile=/etc/etcd/etcd.conf</span><br><span class="line">ExecStart=/usr/bin/etcd \</span><br><span class="line">--name=$&#123;ETCD_NAME&#125; \</span><br><span class="line">--data-dir=$&#123;ETCD_DATA_DIR&#125; \</span><br><span class="line">--listen-peer-urls=$&#123;ETCD_LISTEN_PEER_URLS&#125; \</span><br><span class="line">--listen-client-urls=$&#123;ETCD_LISTEN_CLIENT_URLS&#125;,http://127.0.0.1:2379 \</span><br><span class="line">--advertise-client-urls=$&#123;ETCD_ADVERTISE_CLIENT_URLS&#125; \</span><br><span class="line">--initial-advertise-peer-urls=$&#123;ETCD_INITIAL_ADVERTISE_PEER_URLS&#125; \</span><br><span class="line">--initial-cluster=$&#123;ETCD_INITIAL_CLUSTER&#125; \</span><br><span class="line">--initial-cluster-token=$&#123;ETCD_INITIAL_CLUSTER_TOKEN&#125; \</span><br><span class="line">--initial-cluster-state=new</span><br><span class="line">Restart=on-failure</span><br><span class="line">LimitNOFILE=65536</span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<br/>
<br/>

<h2 id="etcd之HTTPS"><a href="#etcd之HTTPS" class="headerlink" title="etcd之HTTPS"></a>etcd之HTTPS</h2><p><em>购买证书或者自制私有证书,使etcd支持https方式访问</em></p>
<h5 id="安装配置CFSSL"><a href="#安装配置CFSSL" class="headerlink" title="安装配置CFSSL"></a>安装配置CFSSL</h5><p><em>创建证书工具,用于提供https服务, 只需要在master结点上安装</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wget https://pkg.cfssl.org/R1.2/cfssl_linux-amd64</span><br><span class="line">wget https://pkg.cfssl.org/R1.2/cfssljson_linux-amd64</span><br><span class="line">wget https://pkg.cfssl.org/R1.2/cfssl-certinfo_linux-amd64</span><br><span class="line">chmod +x cfssl_linux-amd64 cfssljson_linux-amd64 cfssl-certinfo_linux-amd64</span><br><span class="line">mv cfssl_linux-amd64 /usr/local/bin/cfssl</span><br><span class="line">mv cfssljson_linux-amd64 /usr/local/bin/cfssljson</span><br><span class="line">mv cfssl-certinfo_linux-amd64 /usr/bin/cfssl-certinfo</span><br></pre></td></tr></table></figure>

<h5 id="创建证书"><a href="#创建证书" class="headerlink" title="创建证书"></a>创建证书</h5><p><strong>生成根证书</strong></p>
<p><em>只需在master结点上生成ca证书,然后同步至node结点中</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt; EOF | tee ca-config.json</span><br><span class="line">&#123;</span><br><span class="line">  &quot;signing&quot;: &#123;</span><br><span class="line">    &quot;default&quot;: &#123;</span><br><span class="line">      &quot;expiry&quot;: &quot;87600h&quot;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;profiles&quot;: &#123;</span><br><span class="line">      &quot;www&quot;: &#123;</span><br><span class="line">         &quot;expiry&quot;: &quot;87600h&quot;,</span><br><span class="line">         &quot;usages&quot;: [</span><br><span class="line">            &quot;signing&quot;,</span><br><span class="line">            &quot;key encipherment&quot;,</span><br><span class="line">            &quot;server auth&quot;,</span><br><span class="line">            &quot;client auth&quot;</span><br><span class="line">        ]</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># etcd ca配置文件</span><br><span class="line">cat &lt;&lt; EOF | tee ca-csr.json</span><br><span class="line">&#123;</span><br><span class="line">    &quot;CN&quot;: &quot;etcd CA&quot;,</span><br><span class="line">    &quot;key&quot;: &#123;</span><br><span class="line">        &quot;algo&quot;: &quot;rsa&quot;,</span><br><span class="line">        &quot;size&quot;: 2048</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;names&quot;: [</span><br><span class="line">        &#123;</span><br><span class="line">            &quot;C&quot;: &quot;CN&quot;,</span><br><span class="line">            &quot;L&quot;: &quot;Shanghai&quot;,</span><br><span class="line">            &quot;ST&quot;: &quot;Shanghai&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># etcd server证书</span><br><span class="line"># hosts是etcd结点ip列表</span><br><span class="line">cat &lt;&lt; EOF | tee server-csr.json</span><br><span class="line">&#123;</span><br><span class="line">    &quot;CN&quot;: &quot;etcd&quot;,</span><br><span class="line">    &quot;hosts&quot;: [</span><br><span class="line">        &quot;10.0.2.15&quot;,</span><br><span class="line">        &quot;10.0.2.4&quot;,</span><br><span class="line">        &quot;10.0.2.5&quot;</span><br><span class="line">    ],</span><br><span class="line">    &quot;key&quot;: &#123;</span><br><span class="line">        &quot;algo&quot;: &quot;rsa&quot;,</span><br><span class="line">        &quot;size&quot;: 2048</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;names&quot;: [</span><br><span class="line">        &#123;</span><br><span class="line">            &quot;C&quot;: &quot;CN&quot;,</span><br><span class="line">            &quot;L&quot;: &quot;Shanghai&quot;,</span><br><span class="line">            &quot;ST&quot;: &quot;Shanghai&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cfssl gencert -initca ca-csr.json | cfssljson -bare ca -</span><br><span class="line">cfssl gencert -ca=ca.pem \</span><br><span class="line">    -ca-key=ca-key.pem -config=ca-config.json \</span><br><span class="line">    -profile=www server-csr.json | cfssljson -bare server</span><br></pre></td></tr></table></figure>


<h5 id="配置证书"><a href="#配置证书" class="headerlink" title="配置证书"></a>配置证书</h5><p><em>更新完master结点的service文件之后，同步到node结点</em></p>
<p>编辑文件,<code>vim /lib/systemd/system/etcd.service</code>,在ExecStart指令后面添加如下启动参数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--cert-file=/etc/etcd/ssl/server.pem \</span><br><span class="line">--key-file=/etc/etcd/ssl/server-key.pem \</span><br><span class="line">--peer-cert-file=/etc/etcd/ssl/server.pem \</span><br><span class="line">--peer-key-file=/etc/etcd/ssl/server-key.pem \</span><br><span class="line">--trusted-ca-file=/etc/etcd/ssl/ca.pem \</span><br><span class="line">--peer-trusted-ca-file=/etc/etcd/ssl/ca.pem</span><br></pre></td></tr></table></figure>

<p>编辑文件,<code>vim /etc/etcd/etcd.conf</code>, 更新协议, http -&gt; https</p>
<h2 id="服务健康检测"><a href="#服务健康检测" class="headerlink" title="服务健康检测"></a>服务健康检测</h2><p><em>默认检测的endpoint是<a href="http://0.0.0.0:2379/">http://0.0.0.0:2379</a></em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">etcdctl cluster-health</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>两个链表的第一个公共节点</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E4%B8%A4%E4%B8%AA%E9%93%BE%E8%A1%A8%E7%9A%84%E7%AC%AC%E4%B8%80%E4%B8%AA%E5%85%AC%E5%85%B1%E8%8A%82%E7%82%B9/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>输入两个无环的单向链表，找出它们的第一个公共结点，如果没有公共节点则返回空。（注意因为传入数据是链表，所以错误测试数据的提示是用其他方式显示的，保证传入数据是正确的）</p>
<span id="more"></span>

<p><strong>输入描述</strong>：</p>
<p>输入分为是3段，第一段是第一个链表的非公共部分，第二段是第二个链表的非公共部分，第三段是第一个链表和第二个链表的公共部分。 后台会将这3个参数组装为两个链表，并将这两个链表对应的头节点传入到函数 <strong><code>FindFirstCommonNode</code></strong> 里面，用户得到的输入只有 <strong><code>pHead1</code></strong> 和 <strong><code>pHead2</code></strong> 。</p>
<p><strong>返回值描述</strong>：</p>
<p>返回传入的 <strong><code>pHead1</code></strong> 和 <strong><code>pHead2</code></strong> 的第一个公共结点，后台会打印以该节点为头节点的链表。</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,2,3&#125;,&#123;4,5&#125;,&#123;6,7&#125;</span><br><span class="line">返回:  &#123;6,7&#125;</span><br><span class="line">说明:  第一个参数&#123;1,2,3&#125;代表是第一个链表非公共部分，第二个参数&#123;4,5&#125;代表是第二个链表非公共部分，最后的&#123;6,7&#125;表示的是2个链表的公共部分</span><br><span class="line">这3个参数最后在后台会组装成为2个两个无环的单链表，且是有公共节点的  </span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1&#125;,&#123;2,3&#125;,&#123;&#125;</span><br><span class="line">返回:  &#123;&#125;</span><br><span class="line">说明:  2个链表没有公共节点 ,返回null，后台打印&#123;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<p>使用两个指针 a，b 分别指向两个链表 <strong><code>pHead1</code></strong> ， <strong><code>pHead2</code></strong> 的头结点，然后同时分别逐结点遍历，当 a 到达链表 <strong><code>pHead1</code></strong> 的末尾时，重新定位到链表 <strong><code>pHead2</code></strong> 的头结点；当 b 到达链表 <strong><code>pHead2</code></strong> 的末尾时，重新定位到链表 <strong><code>pHead1</code></strong> 的头结点。当双指针相遇时，所指向的结点就是第一个公共结点</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param pHead1 ListNode类 </span></span><br><span class="line"><span class="comment"> * @param pHead2 ListNode类 </span></span><br><span class="line"><span class="comment"> * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">FindFirstCommonNode</span><span class="params">( pHead1 *ListNode ,  pHead2 *ListNode )</span></span> *ListNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    cur1 := pHead1</span><br><span class="line">    cur2 := pHead2</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> cur1 != cur2 &#123;</span><br><span class="line">        <span class="keyword">if</span> cur1 == <span class="literal">nil</span> &#123;</span><br><span class="line">            cur1 = pHead2</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            cur1 = cur1.Next</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> cur2 == <span class="literal">nil</span> &#123;</span><br><span class="line">            cur2 = pHead1</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            cur2 = cur2.Next</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> cur1</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>通过kube-apiserver访问kube-dashboard</title>
    <url>//2020/05/16/k8s/%E9%80%9A%E8%BF%87apiserver%E8%AE%BF%E9%97%AEdashboard/</url>
    <content><![CDATA[<h1 id="制作证书"><a href="#制作证书" class="headerlink" title="制作证书"></a>制作证书</h1><span id="more"></span>


<p><strong>client-certificate-data</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep &#x27;client-certificate-data&#x27; ~/.kube/config | head -n 1 | awk &#x27;&#123;print $2&#125;&#x27; | base64 -d &gt;&gt; kubecfg.crt</span><br></pre></td></tr></table></figure>


<p><strong>client-key-data</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep &#x27;client-key-data&#x27; ~/.kube/config | head -n 1 | awk &#x27;&#123;print $2&#125;&#x27; | base64 -d &gt;&gt; kubecfg.key</span><br></pre></td></tr></table></figure>


<p><strong>生成ps12文件</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">openssl pkcs12 -export -clcerts -inkey kubecfg.key -in kubecfg.crt -out kubecfg.p12 -name &quot;kubernetes-client&quot;</span><br></pre></td></tr></table></figure>


<blockquote>
<p>tips: 浏览器导入证书之后需要重启</p>
</blockquote>
]]></content>
      <tags>
        <tag>kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>二分查找</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE/</url>
    <content><![CDATA[<p><strong>描述</strong><br>请实现无重复数字的升序数组的二分查找</p>
<p>给定一个 元素升序的、无重复数字的整型数组nums和一个目标值target，写一个函数搜索nums中的 target，如果目标值存在返回下标（下标从 0 开始），否则返回-1</p>
<span id="more"></span>

<p><em>数据范围：</em> <strong>0≤len(nums)≤2*10<sup>5</sup></strong> ,数组中任意值满足 <strong>|val|≤10<sup>9</sup></strong><br><em>要求：</em> 时间复杂度<em>O</em>(<em>log</em>n)，空间复杂度<em>O</em>(1)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [-1,0,3,4,6,10,13,14],13</span><br><span class="line">返回:  6</span><br><span class="line">说明:  13 出现在nums中并且下标为 6</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [],3</span><br><span class="line">返回:  -1</span><br><span class="line">说明:  nums为空，返回-1</span><br></pre></td></tr></table></figure>

<p><strong>示例3</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [-1,0,3,4,6,10,13,14],2</span><br><span class="line">返回:  -1</span><br><span class="line">说明:  2 不存在nums中因此返回 -1</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>分支算法</p>
</blockquote>
<ul>
<li>从数组首尾开始，每次取中点值</li>
<li>如果中间值等于目标即找到了，可返回下标，如果中点值大于目标，说明中点以后的都大于目标，因此目标在中点左半区间，如果中点值小于目标，则相反</li>
<li>根据比较进入对应的区间，直到区间左右端相遇，意味着没有找到</li>
</ul>
<img src="/images/二分查找.gif" alt="二分查找.gif">

<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param nums int整型一维数组</span></span><br><span class="line"><span class="comment"> * @param target int整型</span></span><br><span class="line"><span class="comment"> * @return int整型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">search</span><span class="params">( nums []<span class="type">int</span> ,  target <span class="type">int</span> )</span></span> <span class="type">int</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    left, rigth := <span class="number">0</span>, <span class="built_in">len</span>(nums) - <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> left &lt;= rigth &#123;</span><br><span class="line">        middle := left + (rigth-left)/<span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> nums[middle] == target &#123;</span><br><span class="line">            <span class="keyword">return</span> middle</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> nums[middle] &gt; target &#123;</span><br><span class="line">            rigth = middle - <span class="number">1</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            left = middle + <span class="number">1</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二分查找</tag>
      </tags>
  </entry>
  <entry>
    <title>二维数组中的查找</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E4%BA%8C%E4%BD%8D%E6%95%B0%E7%BB%84%E4%B8%AD%E7%9A%84%E6%9F%A5%E6%89%BE/</url>
    <content><![CDATA[<p><strong>描述</strong><br>在一个二维数组array中（每个一维数组的长度相同），每一行都按照从左到右递增的顺序排序，每一列都按照从上到下递增的顺序排序。请完成一个函数，输入这样的一个二维数组和一个整数，判断数组中是否含有该整数。</p>
<span id="more"></span>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[</span><br><span class="line">    [1,2,8,9],</span><br><span class="line">    [2,4,9,12],</span><br><span class="line">    [4,7,10,13],</span><br><span class="line">    [6,8,11,15]</span><br><span class="line">]</span><br></pre></td></tr></table></figure>
<p>给定 target &#x3D; 7，返回 true。<br>给定 target &#x3D; 3，返回 false。</p>
<p><em>数据范围：</em> 矩阵的长宽满足<code>0≤n,m≤500</code>，矩阵中的值满足0≤val≤10<sup>9</sup><br><em>要求：</em> 空间复杂度<em>O</em>(1)，时间复杂度<em>O</em>(n+m)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  7,[[1,2,8,9],[2,4,9,12],[4,7,10,13],[6,8,11,15]]</span><br><span class="line">返回:  true</span><br><span class="line">说明:  存在7，返回true</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  1,[[2]]</span><br><span class="line">返回:  false</span><br></pre></td></tr></table></figure>

<p><strong>示例3</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  3,[[1,2,8,9],[2,4,9,12],[4,7,10,13],[6,8,11,15]]</span><br><span class="line">返回:  false</span><br><span class="line">说明:  不存在3，返回false </span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<p>每一列都是按照从上到下递增的顺序排序，那么直接用每一列的开始和结尾和target比较，如果target在区间内，就遍历该列，找到target就返回true，否则返回false</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param target int整型 </span></span><br><span class="line"><span class="comment"> * @param array int整型二维数组 </span></span><br><span class="line"><span class="comment"> * @return bool布尔型</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Find</span><span class="params">( target <span class="type">int</span> ,  array [][]<span class="type">int</span> )</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    rows := <span class="built_in">len</span>(array)</span><br><span class="line">    <span class="keyword">if</span> rows == <span class="number">0</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    j := <span class="built_in">len</span>(array[<span class="number">0</span>]) - <span class="number">1</span></span><br><span class="line">    i := <span class="number">0</span></span><br><span class="line">    <span class="comment">// todo: 遍历区间时是暴力遍历,可以修改为二分查找</span></span><br><span class="line">    <span class="keyword">for</span> i &lt; rows &amp;&amp; j &gt;= <span class="number">0</span> &#123;</span><br><span class="line">        v := array[i][j]</span><br><span class="line">        <span class="keyword">if</span> v &gt; target &#123;</span><br><span class="line">            j--</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> v &lt; target &#123;</span><br><span class="line">            i++</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二分查找</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉搜索树与双向链表</title>
    <url>//2023/05/15/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91%E4%B8%8E%E5%8F%8C%E5%90%91%E9%93%BE%E8%A1%A8/</url>
    <content><![CDATA[<p><strong>描述</strong><br>输入一棵二叉搜索树，将该二叉搜索树转换成一个排序的双向链表。如下图所示</p>
<span id="more"></span>
<p><img src="/images/%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91%E4%B8%8E%E5%8F%8C%E5%90%91%E9%93%BE%E8%A1%A8-%E7%A4%BA%E4%BE%8B.png"></p>
<p>注意:</p>
<ol>
<li>要求不能创建任何新的结点，只能调整树中结点指针的指向。当转化完成以后，树中节点的左指针需要指向前驱，树中节点的右指针需要指向后继</li>
<li>返回链表中的第一个节点的指针</li>
<li>函数返回的TreeNode，有左右指针，其实可以看成一个双向链表的数据结构</li>
</ol>
<p><strong>输入描述：</strong><br>二叉树的根节点</p>
<p><strong>返回值描述：</strong><br>双向链表的其中一个头节点。</p>
<p><strong>题解</strong></p>
<p>已知将二叉搜索树进行中序遍历可以得到由小到大的顺序排列，因此最直接的想法就是进行中序遍历。将中序遍历的结果用数组存储下来，得到的数组是有从小到大顺序的。最后将数组中的结点依次连接即可</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param pRootOfTree TreeNode类 </span></span><br><span class="line"><span class="comment"> * @return TreeNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Convert</span><span class="params">( pRootOfTree *TreeNode )</span></span> *TreeNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> pRootOfTree == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line">    nodes := []*TreeNode&#123;&#125;</span><br><span class="line">    inorder(&amp;nodes, pRootOfTree)</span><br><span class="line"></span><br><span class="line">    head := &amp;TreeNode&#123;Val: nodes[<span class="number">0</span>].Val&#125;</span><br><span class="line"></span><br><span class="line">    preHead := head</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> index := <span class="number">1</span>; index &lt; <span class="built_in">len</span>(nodes); index++ &#123;</span><br><span class="line">        head.Right = nodes[index]</span><br><span class="line">        nodes[index].Left = head</span><br><span class="line">        head = head.Right</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> preHead</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">inorder</span><span class="params">(nodes *[]*TreeNode, root *TreeNode)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    inorder(nodes, root.Left)</span><br><span class="line">    *nodes = <span class="built_in">append</span>(*nodes, root)</span><br><span class="line">    inorder(nodes, root.Right)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树中和为某一值的路径(一)</title>
    <url>//2023/05/15/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%8F%89%E6%A0%91%E4%B8%AD%E5%92%8C%E4%B8%BA%E6%9F%90%E4%B8%80%E5%80%BC%E7%9A%84%E8%B7%AF%E5%BE%84(%E4%B8%80)/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一个二叉树root和一个值 sum ，判断是否有从根节点到叶子节点的节点值之和等于 sum 的路径。</p>
<span id="more"></span>

<ol>
<li>该题路径定义为从树的根结点开始往下一直到叶子结点所经过的结点</li>
<li>叶子节点是指没有子节点的节点</li>
<li>路径只能从父节点到子节点，不能从子节点到父节点</li>
<li>总节点数目为n</li>
</ol>
<p><strong>题解</strong></p>
<ul>
<li>递归<blockquote>
<p>采用递归遍历二叉树的路径节点，同时计算二叉树路径节点的数字之和，当到达叶子节点且路径的数字之和等于 sum 则说明二叉树中存在节点和为指定值的路径</p>
</blockquote>
</li>
</ul>
<ol>
<li>特殊情况：当二叉树为空，则返回 false</li>
<li>遍历根节点的左右子树，记录根节点的数字之和 res，当节点的左右子树均为空，且 res &#x3D;&#x3D; sum，则返回 true</li>
<li>递归 该节点的左右子树，做上述计算</li>
</ol>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * </span></span><br><span class="line"><span class="comment">  * @param root TreeNode类 </span></span><br><span class="line"><span class="comment">  * @param sum int整型 </span></span><br><span class="line"><span class="comment">  * @return bool布尔型</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">hasPathSum</span><span class="params">( root *TreeNode ,  sum <span class="type">int</span> )</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> root.Left == <span class="literal">nil</span> &amp;&amp; root.Right == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> root.Val == sum</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> hasPathSum(root.Left, sum-root.Val) || hasPathSum(root.Right, sum - root.Val)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<ul>
<li>深度优先遍历+回溯</li>
</ul>
<ol>
<li>首先，深度优点遍历来说，先写上一个回溯 if (curNode &#x3D;&#x3D; null) { return false; }，这表示递归至最深层开始回溯</li>
<li>每次进入函数时，将 sum 减去当前节点的权重(curNode.val)，当 sum 减到零时，说明目标路径存在，另外我们的目标是到叶子节点停止，叶子节点的条件是 curNode.left &#x3D;&#x3D; null &amp;&amp; curNode.right &#x3D;&#x3D; null，所以说当 if (curNode.left &#x3D;&#x3D; null &amp;&amp; curNode.right &#x3D;&#x3D; null &amp;&amp; target &#x3D;&#x3D; 0) ，我们返回 true 表示找到目标路径</li>
<li>深度遍历的分支：对于当前节点 curNode 有两个分支，这两个分支都有可能成为目标路径，所以深度优先遍历的写法为 return dfs(curNode.left, target) || dfs(curNode.right, target)</li>
<li>现在来谈谈为什么回溯时需要返回 false，因为当 curNode 为叶子节点时，并且 sum &#x3D;&#x3D; 0 时，我们已经返回了 true，剩下的情况就是 curNode 不是叶子节点或者路径值不为 target，所应该返回 false</li>
</ol>
<p><img src="/images/%E4%BA%8C%E5%8F%89%E6%A0%91%E5%92%8C%E4%B8%BA%E6%9F%90%E4%B8%80%E5%80%BC%E7%9A%84%E8%B7%AF%E5%BE%84(%E4%B8%80)%E4%B9%8B%E6%B7%B1%E5%BA%A6%E4%BC%98%E5%85%88%E9%81%8D%E5%8E%86%E5%92%8C%E5%9B%9E%E6%BA%AF.png"></p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param root TreeNode类</span></span><br><span class="line"><span class="comment"> * @param sum int整型</span></span><br><span class="line"><span class="comment"> * @return bool布尔型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">hasPathSum</span><span class="params">(root *TreeNode, sum <span class="type">int</span>)</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">	<span class="comment">// write code here</span></span><br><span class="line">	<span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> dfs(root, sum)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">dfs</span><span class="params">(node *TreeNode, target <span class="type">int</span>)</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">	<span class="comment">// 目标路径不存在，开始回溯</span></span><br><span class="line">	<span class="keyword">if</span> node == <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 更新目标值</span></span><br><span class="line">	target -= node.Val</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 当当前节点为叶子节点并且目标路径存在时，返回 true</span></span><br><span class="line">	<span class="keyword">if</span> node.Left == <span class="literal">nil</span> &amp;&amp; node.Right == <span class="literal">nil</span> &amp;&amp; target == <span class="number">0</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 对左右分支进行 dfs</span></span><br><span class="line">	<span class="keyword">return</span> dfs(node.Left, target) || dfs(node.Right, target)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树的中序遍历</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E4%B8%AD%E5%BA%8F%E9%81%8D%E5%8E%86/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一个二叉树的根节点root，返回它的中序遍历结果。</p>
<span id="more"></span>

<p><strong>题解</strong></p>
<ul>
<li>遍历左子树</li>
<li>打印根节点</li>
<li>遍历右子树</li>
</ul>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param root TreeNode类 </span></span><br><span class="line"><span class="comment"> * @return int整型一维数组</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">inorderTraversal</span><span class="params">( root *TreeNode )</span></span> []<span class="type">int</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    inorderVals := <span class="built_in">make</span>([]<span class="type">int</span>, <span class="number">0</span>)</span><br><span class="line">    inorder(&amp;inorderVals, root)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> inorderVals</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">inorder</span><span class="params">(res *[]<span class="type">int</span>, root *TreeNode)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    inorder(res, root.Left)</span><br><span class="line">    *res = <span class="built_in">append</span>(*res, root.Val)</span><br><span class="line">    inorder(res, root.Right)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树的后序遍历</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E5%90%8E%E5%BA%8F%E9%81%8D%E5%8E%86/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一个二叉树，返回他的后序遍历的序列。</p>
<span id="more"></span>

<p><strong>题解</strong></p>
<ul>
<li>遍历左子树</li>
<li>遍历右子树</li>
<li>打印根节点</li>
</ul>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param root TreeNode类 </span></span><br><span class="line"><span class="comment"> * @return int整型一维数组</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">postorderTraversal</span><span class="params">( root *TreeNode )</span></span> []<span class="type">int</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    postorderVals := <span class="built_in">make</span>([]<span class="type">int</span>, <span class="number">0</span>)</span><br><span class="line">    postorder(&amp;postorderVals, root)</span><br><span class="line">    <span class="keyword">return</span> postorderVals</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">postorder</span><span class="params">(res *[]<span class="type">int</span>, root *TreeNode)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    postorder(res, root.Left)</span><br><span class="line">    postorder(res, root.Right)</span><br><span class="line">    *res = <span class="built_in">append</span>(*res, root.Val)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树的层序遍历</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E5%B1%82%E5%BA%8F%E9%81%8D%E5%8E%86/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一个二叉树，返回该二叉树层序遍历的结果，（从左到右，一层一层地遍历）</p>
<span id="more"></span>

<p><strong>题解</strong></p>
<ul>
<li>首先判断二叉树是否为空，空树没有遍历结果</li>
<li>建立辅助队列，根节点首先进入队列。不管层次怎么访问，根节点一定是第一个，那它肯定排在队伍的最前面</li>
<li>每次进入一层，统计队列中元素的个数。因为每当访问完一层，下一层作为这一层的子节点，一定都加入队列，而再下一层还没有加入，因此此时队列中的元素个数就是这一层的元素个数</li>
<li>每次遍历这一层这么多的节点数，将其依次从队列中弹出，然后加入这一行的一维数组中，如果它们有子节点，依次加入队列排队等待访问</li>
<li>访问完这一层的元素后，将这个一维数组加入二维数组中，再访问下一层</li>
</ul>
<img src="/images/二叉树的层序遍历.gif" alt="二叉树的层序遍历.gif">

<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * </span></span><br><span class="line"><span class="comment">  * @param root TreeNode类 </span></span><br><span class="line"><span class="comment">  * @return int整型二维数组</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">levelOrder</span><span class="params">( root *TreeNode )</span></span> [][]<span class="type">int</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    queues := []*TreeNode&#123;&#125;</span><br><span class="line">    queues = <span class="built_in">append</span>(queues, root)</span><br><span class="line">    levels := [][]<span class="type">int</span>&#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> <span class="built_in">len</span>(queues) &gt; <span class="number">0</span> &#123;</span><br><span class="line">        n := <span class="built_in">len</span>(queues)</span><br><span class="line">        level := []<span class="type">int</span>&#123;&#125;</span><br><span class="line">        <span class="keyword">for</span> index :=<span class="number">0</span>; index &lt; n; index++ &#123;</span><br><span class="line">            root := queues[<span class="number">0</span>]</span><br><span class="line">            queues = queues[<span class="number">1</span>:]</span><br><span class="line">            level = <span class="built_in">append</span>(level, root.Val)</span><br><span class="line">            <span class="keyword">if</span> root.Left != <span class="literal">nil</span> &#123;</span><br><span class="line">                queues = <span class="built_in">append</span>(queues, root.Left)</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> root.Right != <span class="literal">nil</span> &#123;</span><br><span class="line">                queues = <span class="built_in">append</span>(queues, root.Right)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        levels = <span class="built_in">append</span>(levels, level)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> levels</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树的最大深度</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E6%9C%80%E5%A4%A7%E6%B7%B1%E5%BA%A6/</url>
    <content><![CDATA[<p><strong>描述</strong><br>求给定二叉树的最大深度，深度是指树的根节点到任一叶子节点路径上节点的数量。最大深度是所有叶子节点的深度的最大值。</p>
<span id="more"></span>

<p><strong>题解</strong></p>
<blockquote>
<p>DFS</p>
</blockquote>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param root TreeNode类</span></span><br><span class="line"><span class="comment"> * @return int整型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">maxDepth</span><span class="params">( root *TreeNode )</span></span> <span class="type">int</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> max(maxDepth(root.Left), maxDepth(root.Right)) + <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">max</span><span class="params">(left, rigth <span class="type">int</span>)</span></span> <span class="type">int</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> left &gt; rigth &#123;</span><br><span class="line">        <span class="keyword">return</span> left</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> rigth</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树的前序遍历</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E5%89%8D%E5%BA%8F%E9%81%8D%E5%8E%86/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给你二叉树的根节点root，返回它节点值的前序遍历。</p>
<span id="more"></span>

<p><strong>题解</strong></p>
<ul>
<li>先打印根节点</li>
<li>遍历左子树</li>
<li>遍历右子树</li>
</ul>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param root TreeNode类</span></span><br><span class="line"><span class="comment"> * @return int整型一维数组</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">preorderTraversal</span><span class="params">(root *TreeNode)</span></span> []<span class="type">int</span> &#123;</span><br><span class="line">	<span class="comment">// write code here</span></span><br><span class="line">	orderVal := <span class="built_in">make</span>([]<span class="type">int</span>, <span class="number">0</span>)</span><br><span class="line">	preOrder(&amp;orderVal, root)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> orderVal</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">preOrder</span><span class="params">(res *[]<span class="type">int</span>, root *TreeNode)</span></span> &#123;</span><br><span class="line">	<span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125;</span><br><span class="line">	*res = <span class="built_in">append</span>(*res, root.Val)</span><br><span class="line">	preOrder(res, root.Left)</span><br><span class="line">	preOrder(res, root.Right)</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树的镜像</title>
    <url>//2023/05/16/%E7%AE%97%E6%B3%95/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%95%9C%E5%83%8F/</url>
    <content><![CDATA[<p><strong>描述</strong><br>操作给定的二叉树，将其变换为源二叉树的镜像。</p>
<span id="more"></span>

<p>例如:</p>
<ul>
<li>源二叉树<br><img src="/images/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%95%9C%E5%83%8F-%E7%A4%BA%E4%BE%8B1.png"></li>
<li>镜像二叉树<br><img src="/images/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%95%9C%E5%83%8F-%E7%A4%BA%E4%BE%8B2.png"></li>
</ul>
<p><strong>题解</strong><br>根据二叉树镜像的定义，考虑递归遍历（dfs）二叉树，交换每个节点的左 &#x2F; 右子节点，即可生成二叉树的镜像</p>
<ul>
<li>特判：如果pRoot为空，返回空</li>
<li>交换左右子树</li>
<li>把pRoot的左子树放到Mirror中镜像一下</li>
<li>把pRoot的右子树放到Mirror中镜像一下</li>
<li>返回根节点root<br><img src="/images/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%95%9C%E5%83%8F.png"></li>
</ul>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param pRoot TreeNode类 </span></span><br><span class="line"><span class="comment"> * @return TreeNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Mirror</span><span class="params">( pRoot *TreeNode )</span></span> *TreeNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> pRoot == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    left := Mirror(pRoot.Left)</span><br><span class="line">    rigth := Mirror(pRoot.Right)</span><br><span class="line"></span><br><span class="line">    pRoot.Left = rigth</span><br><span class="line">    pRoot.Right = left</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> pRoot</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>删除有序链表中重复的元素(一)</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E5%88%A0%E9%99%A4%E6%9C%89%E5%BA%8F%E9%93%BE%E8%A1%A8%E4%B8%AD%E9%87%8D%E5%A4%8D%E7%9A%84%E5%85%83%E7%B4%A01/</url>
    <content><![CDATA[<p><strong>描述</strong><br>删除给出链表中的重复元素（链表中元素从小到大有序），使链表中的所有元素都只出现一次</p>
<span id="more"></span>

<p>例如:<br>给出的链表 <code>1→1→1→2→2→4</code>，返回<code>1→2→4</code>。<br>给出的链表 <code>1→1→2→3</code>，返回<code>1→2→3</code>。</p>
<p><em>数据范围：</em> 链表长度满足<code>0≤n≤100</code>，链表中任意节点的值满足<code>|val|≤100</code><br><em>要求：</em> 空间复杂度<em>O</em>(1)，时间复杂度<em>O</em>(n)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,1,2&#125;</span><br><span class="line">返回:  &#123;1,2&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;&#125;</span><br><span class="line">返回:  &#123;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<ul>
<li>判断链表是否为空链表，空链表不处理直接返回</li>
<li>使用一个指针遍历链表，如果指针当前节点与下一个节点的值相同，我们就跳过下一个节点，当前节点直接连接下个节点的后一位</li>
<li>如果当前节点与下一个节点值不同，继续往后遍历</li>
<li>循环过程中每次用到了两个节点值，要检查连续两个节点是否为空</li>
</ul>
<img src="/images/删除有序链表中重复的元素(1).gif" alt="删除有序链表中重复的元素(1).gif">

<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * </span></span><br><span class="line"><span class="comment">  * @param head ListNode类 </span></span><br><span class="line"><span class="comment">  * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">deleteDuplicates</span><span class="params">( head *ListNode )</span></span> *ListNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> head == <span class="literal">nil</span> || head.Next == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> head</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cur := head</span><br><span class="line">    <span class="keyword">for</span> cur.Next != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> cur.Val == cur.Next.Val &#123;</span><br><span class="line">            cur.Next = cur.Next.Next</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            cur = cur.Next</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> head</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>删除有序链表中重复的元素(二)</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E5%88%A0%E9%99%A4%E6%9C%89%E5%BA%8F%E9%93%BE%E8%A1%A8%E4%B8%AD%E9%87%8D%E5%A4%8D%E7%9A%84%E5%85%83%E7%B4%A02/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给出一个升序排序的链表，删除链表中的所有重复出现的元素，只保留原链表中只出现一次的元素。</p>
<span id="more"></span>

<p>例如:<br>给出的链表<code>1→1→1→2→2→4</code>，返回<code>4</code>。<br>给出的链表<code>1→1→1→2→3</code>，返回<code>2→3</code>。</p>
<p><em>数据范围：</em> 链表长度满足<code>0≤n≤10000</code>，链表中任意节点的值满足<code>|val|≤1000</code><br><em>要求：</em> 空间复杂度<em>O</em>(n)，时间复杂度<em>O</em>(n)<br><em>进阶：</em> 空间复杂度<em>O</em>(1)，时间复杂度<em>O</em>(n)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,2,2&#125;</span><br><span class="line">返回:  &#123;1&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;&#125;</span><br><span class="line">返回:  &#123;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<ul>
<li>给链表前加上表头，方便可能的话删除第一个节点</li>
<li>遍历链表，每次比较相邻两个节点，如果遇到了两个相邻节点相同，则新开内循环将这一段所有的相同都遍历过去</li>
<li>在step 2中这一连串相同的节点前的节点直接连上后续第一个不相同值的节点</li>
<li>返回时去掉添加的表头</li>
</ul>
<img src="/images/删除有序链表中重复的元素(2).gif" alt="删除有序链表中重复的元素(2).gif">

<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * </span></span><br><span class="line"><span class="comment">  * @param head ListNode类 </span></span><br><span class="line"><span class="comment">  * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">deleteDuplicates</span><span class="params">( head *ListNode )</span></span> *ListNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> head == <span class="literal">nil</span> || head.Next == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> head</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    dummy := &amp;ListNode&#123;Next: head&#125;</span><br><span class="line">    pre := dummy</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> pre.Next != <span class="literal">nil</span> &amp;&amp; pre.Next.Next != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> pre.Next.Val == pre.Next.Next.Val &#123;</span><br><span class="line">            x := pre.Next.Val</span><br><span class="line">            <span class="keyword">for</span> pre.Next != <span class="literal">nil</span> &amp;&amp; pre.Next.Val == x &#123;</span><br><span class="line">                pre.Next = pre.Next.Next</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            pre = pre.Next</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> dummy.Next</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>删除链表中倒数第k个节点</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E5%88%A0%E9%99%A4%E9%93%BE%E8%A1%A8%E4%B8%AD%E5%80%92%E6%95%B0%E7%AC%ACk%E4%B8%AA%E8%8A%82%E7%82%B9/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>给定一个链表，删除链表的倒数第 n 个节点并返回链表的头指针</p>
<span id="more"></span>
<p>例如，</p>
<p>给出链表 <strong><code>1→2→3→4→5，n=2</code></strong></p>
<p>删除了链表中倒数第n个节点之后，链表变成 <strong><code>1→2→3→5</code></strong></p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,2&#125;,2</span><br><span class="line">返回:  &#123;2&#125; </span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>虚拟头节点</p>
</blockquote>
<p>可以通过快指针先走K步 慢指针先指向head，导致 快指针和慢指针相差n个结点，然后快指针移到末尾 这个时候慢指针就是倒数第n个结点的前一个节点，然后<code>slow.Next = slow.Next.Next</code>删除倒数第n个节点。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * </span></span><br><span class="line"><span class="comment">  * @param head ListNode类 </span></span><br><span class="line"><span class="comment">  * @param n int整型 </span></span><br><span class="line"><span class="comment">  * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">removeNthFromEnd</span><span class="params">( head *ListNode ,  n <span class="type">int</span> )</span></span> *ListNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    dummy := &amp;ListNode&#123;Next: head&#125;</span><br><span class="line">    fast, slow := head, dummy</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; n; i++ &#123;</span><br><span class="line">        fast = fast.Next</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> fast != <span class="literal">nil</span> &#123;</span><br><span class="line">        fast = fast.Next</span><br><span class="line">        slow = slow.Next</span><br><span class="line">    &#125;</span><br><span class="line">    slow.Next = slow.Next.Next</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> dummy.Next</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>判断一个链表是否回文结构</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E5%88%A4%E6%96%AD%E4%B8%80%E4%B8%AA%E9%93%BE%E8%A1%A8%E6%98%AF%E5%90%A6%E5%9B%9E%E6%96%87%E7%BB%93%E6%9E%84/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>给定一个链表，请判断该链表是否为回文结构。回文是指该字符串正序逆序完全一致。</p>
<span id="more"></span>

<p><strong>题解</strong></p>
<blockquote>
<p>快慢指针</p>
</blockquote>
<ol>
<li>快慢指针将链表一分为二</li>
<li>后半部分链表反转</li>
<li>比较两个链表是否一致</li>
</ol>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">isPail</span><span class="params">(head *ListNode)</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">	<span class="keyword">if</span> head == <span class="literal">nil</span> || head.Next == <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">//1，找链表中点，双数就是后面的数</span></span><br><span class="line">	fast, slow := head, head</span><br><span class="line">	<span class="keyword">for</span> fast != <span class="literal">nil</span> &amp;&amp; fast.Next != <span class="literal">nil</span> &#123;</span><br><span class="line">		fast = fast.Next.Next</span><br><span class="line">		slow = slow.Next</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	<span class="comment">//2，翻转链表</span></span><br><span class="line">	<span class="keyword">var</span> pre *ListNode</span><br><span class="line">	cur := slow</span><br><span class="line">	<span class="keyword">for</span> cur != <span class="literal">nil</span> &#123;</span><br><span class="line">		temp := cur.Next</span><br><span class="line">		cur.Next = pre</span><br><span class="line">		pre = cur</span><br><span class="line">		cur = temp</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">//3，比较链表值大小</span></span><br><span class="line">	mid := pre</span><br><span class="line">	<span class="keyword">for</span> mid != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> mid.Val != head.Val &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">		&#125;</span><br><span class="line">		mid = mid.Next</span><br><span class="line">		head = head.Next</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>判断链表中是否有环</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E5%88%A4%E6%96%AD%E9%93%BE%E8%A1%A8%E4%B8%AD%E6%98%AF%E5%90%A6%E6%9C%89%E7%8E%AF/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>判断给定的链表中是否有环。如果有环则返回true，否则返回false。</p>
<span id="more"></span>

<p><em>数据范围：</em> 链表长度 <strong><code>0≤n≤10000</code></strong> ，链表中任意节点的值满足 <strong><code>|val|≤100000</code></strong></p>
<p>要求：空间复杂度 <em>O</em>(1)，时间复杂度 <em>O</em>(<em>n</em>)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;3,2,0,-4&#125;,1</span><br><span class="line">返回:  true</span><br><span class="line">说明:  第一部分&#123;3,2,0,-4&#125;代表一个链表，第二部分的1表示，-4到位置1（注：头结点为位置0），即-4-&gt;2存在一个链接，组成传入的head为一个带环的链表，返回true</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1&#125;,-1</span><br><span class="line">返回:  false</span><br><span class="line">说明:  第一部分&#123;1&#125;代表一个链表，-1代表无环，组成传入head为一个无环的单链表，返回false </span><br></pre></td></tr></table></figure>

<p><strong>示例3</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;-1,-7,7,-4,19,6,-9,-5,-2,-5&#125;,6</span><br><span class="line">返回:  true</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>快慢指针</p>
</blockquote>
<p>我们使用两个指针，fast 与 slow。</p>
<p>它们起始都位于链表的头部。随后，slow 指针每次向后移动一个位置，而fast 指针向后移动两个位置。如果链表中存在环，则 fast 指针最终将再次与 slow 指针在环中相遇。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param head ListNode类 </span></span><br><span class="line"><span class="comment"> * @return bool布尔型</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">hasCycle</span><span class="params">( head *ListNode )</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    slow := head</span><br><span class="line">    fast := head</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> fast != <span class="literal">nil</span> &amp;&amp; fast.Next != <span class="literal">nil</span> &#123;</span><br><span class="line">        slow = slow.Next</span><br><span class="line">        fast = fast.Next.Next</span><br><span class="line">        <span class="keyword">if</span> slow == fast &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>单链表排序</title>
    <url>//2023/05/08/%E7%AE%97%E6%B3%95/%E5%8D%95%E9%93%BE%E8%A1%A8%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>给定一个节点数为n的无序单链表，对其按升序排序。</p>
<span id="more"></span>

<p><strong>示例</strong>1</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [1,3,2,4,5]</span><br><span class="line">返回:  &#123;1,2,3,4,5&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [-1,0,-2]</span><br><span class="line">返回:  &#123;-2,-1,0&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>分治算法</p>
</blockquote>
<ol>
<li>快慢指针将链表一分为二</li>
<li>分别对两个子链表排序</li>
<li>合并排序后的两个链表</li>
</ol>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param head ListNode类 the head node</span></span><br><span class="line"><span class="comment"> * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">sortInList</span><span class="params">( head *ListNode )</span></span> *ListNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> head == <span class="literal">nil</span> || head.Next == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> head        </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    fast, slow := head, head</span><br><span class="line">    <span class="keyword">for</span> fast.Next != <span class="literal">nil</span> &amp;&amp; fast.Next.Next != <span class="literal">nil</span> &#123;</span><br><span class="line">        fast = fast.Next.Next</span><br><span class="line">        slow = slow.Next</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    second_head := slow.Next</span><br><span class="line">    slow.Next = <span class="literal">nil</span></span><br><span class="line">    l1, l2 := sortInList(head), sortInList(second_head)</span><br><span class="line"></span><br><span class="line">    dummy := &amp;ListNode&#123;&#125;</span><br><span class="line">    pre := dummy</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> l1 != <span class="literal">nil</span> &amp;&amp; l2 != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> l1.Val &gt; l2.Val &#123;</span><br><span class="line">            pre.Next = l2</span><br><span class="line">            l2 = l2.Next</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            pre.Next = l1</span><br><span class="line">            l1 = l1.Next</span><br><span class="line">        &#125;</span><br><span class="line">        pre = pre.Next</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> l1 == <span class="literal">nil</span> &#123;</span><br><span class="line">        pre.Next = l2</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> l2 == <span class="literal">nil</span> &#123;</span><br><span class="line">        pre.Next = l1</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> dummy.Next</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>反转链表</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E5%8F%8D%E8%BD%AC%E9%93%BE%E8%A1%A8/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>给定一个单链表的头结点 <strong><code>pHead</code></strong> (该头节点是有值的，比如在下图，它的val是1)，长度为n，反转该链表后，返回新链表的表头。</p>
<span id="more"></span>

<p><em>数据范围</em>: <strong><code>0≤n≤1000</code></strong></p>
<p><em>要求</em>：空间复杂度 <em>O</em>(1) ，时间复杂度 <em>O</em>(<em>n</em>) </p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,2,3&#125;</span><br><span class="line">返回:  &#123;3,2,1&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入: &#123;&#125;</span><br><span class="line">返回: &#123;&#125;</span><br><span class="line">说明: 空链表则输出空 </span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p><strong><code>newHead</code></strong> 表示已反转链表头部<br><strong><code>pHead</code></strong> 表示当前链表发转的节点<br><strong><code>pNext</code></strong> 表示未反转链表的头部保存</p>
</blockquote>
<p><strong><code>pHead</code></strong> 的下一个节点</p>
<p>当链表反转时,需如下步骤:</p>
<ol>
<li>保存 <strong><code>pHead</code></strong> 的下一个节点. 防止后续链表丢失</li>
<li>将 <strong><code>pHead</code></strong> 指向 <strong><code>newHead</code></strong> ,完成节点反转</li>
<li>更新 <strong><code>newHead</code></strong> 为 <strong><code>pHead</code></strong> ,将完成的反转节点加入链表</li>
<li>更新 <strong><code>pHead</code></strong> ,继续反转依旧未反转的链表</li>
</ol>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param pHead ListNode类</span></span><br><span class="line"><span class="comment"> * @return ListNode类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">ReverseList</span><span class="params">(pHead *ListNode)</span></span> *ListNode &#123;</span><br><span class="line">	<span class="keyword">if</span> pHead == <span class="literal">nil</span> || pHead.Next == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> pHead</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> newHead *ListNode</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> pHead != <span class="literal">nil</span> &#123;</span><br><span class="line">        pHead, pHead.Next, newHead = pHead.Next, newHead, pHead</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> newHead</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>合并K个已排序的链表</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E5%90%88%E5%B9%B6K%E4%B8%AA%E5%B7%B2%E6%8E%92%E5%BA%8F%E7%9A%84%E9%93%BE%E8%A1%A8/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>合并 k 个升序的链表并将结果作为一个升序的链表返回其头节点。</p>
<span id="more"></span>

<p><em>数据范围：</em> 节点总数 <strong><code>0≤n≤5000</code></strong> ，每个节点的val满足 <strong><code>|val|≤1000</code></strong></p>
<p>要求：时间复杂度 <em>O</em>(<em><code>nlogn</code></em>)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [&#123;1,2,3&#125;,&#123;4,5,6,7&#125;]</span><br><span class="line">返回:  &#123;1,2,3,4,5,6,7&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [&#123;1,2&#125;,&#123;1,4,5&#125;,&#123;6&#125;]</span><br><span class="line">返回:  &#123;1,1,2,4,5,6&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>递归、分治</p>
</blockquote>
<blockquote>
<p>对于这k个链表，就相当于上述合并阶段的k个子问题，需要划分为链表数量更少的子问题，直到每一组合并时是两两合并，然后继续往上合并，这个过程基于递归：</p>
<ul>
<li><strong>终止条件：</strong> 划分的时候直到左右区间相等或左边大于右边。</li>
<li><strong>返回值：</strong> 每级返回已经合并好的子问题链表。</li>
<li><strong>本级任务：</strong> 对半划分，将划分后的子问题合并成新的链表。</li>
</ul>
</blockquote>
<ol>
<li>从链表数组的首和尾开始，每次划分从中间开始划分，划分成两半，得到左边 <strong><code>n/2</code><strong>个链表和右边</strong><code>n/2</code></strong> 个链表</li>
<li>继续不断递归划分，直到每部分链表数为1.</li>
<li>将划分好的相邻两部分链表，按照<a href="https://www.nowcoder.com/practice/a479a3f0c4554867b35356e0d57cf03d?tpId=295&sfm=html&channel=nowcoder">两个有序链表合并</a>的方式合并，合并好的两部分继续往上合并，直到最终合并成一个链表。</li>
</ol>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param lists ListNode类一维数组</span></span><br><span class="line"><span class="comment"> * @return ListNode类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">mergeKLists</span><span class="params">(lists []*ListNode)</span></span> *ListNode &#123;</span><br><span class="line">	<span class="comment">// write code here</span></span><br><span class="line">	length := <span class="built_in">len</span>(lists)</span><br><span class="line">	<span class="keyword">if</span> length == <span class="number">0</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> length == <span class="number">1</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> lists[<span class="number">0</span>]</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> length == <span class="number">2</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> merge(lists[<span class="number">0</span>], lists[<span class="number">1</span>])</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> merge(mergeKLists(lists[:length/<span class="number">2</span>]), mergeKLists(lists[length/<span class="number">2</span>:]))</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">merge</span><span class="params">(lists1 *ListNode, lists2 *ListNode)</span></span> *ListNode &#123;</span><br><span class="line">	head := &amp;ListNode&#123;&#125;</span><br><span class="line">	cur := head</span><br><span class="line">	<span class="keyword">for</span> lists1 != <span class="literal">nil</span> &amp;&amp; lists2 != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> lists1.Val &lt; lists2.Val &#123;</span><br><span class="line">			cur.Next = lists1</span><br><span class="line">			lists1 = lists1.Next</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			cur.Next = lists2</span><br><span class="line">			lists2 = lists2.Next</span><br><span class="line">		&#125;</span><br><span class="line">		cur = cur.Next</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> lists1 != <span class="literal">nil</span> &#123;</span><br><span class="line">		cur.Next = lists1</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> lists2 != <span class="literal">nil</span> &#123;</span><br><span class="line">		cur.Next = lists2</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> head.Next</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>合并两个排序的链表</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E5%90%88%E5%B9%B6%E4%B8%A4%E4%B8%AA%E6%8E%92%E5%BA%8F%E7%9A%84%E9%93%BE%E8%A1%A8/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>输入两个递增的链表，单个链表的长度为n，合并这两个链表并使新链表中的节点仍然是递增排序的</p>
<span id="more"></span>

<p><em>数据范围</em>： <strong><code>0≤n≤1000</code></strong> ， <strong><code>-1000≤节点值≤1000</code></strong> </p>
<p><em>要求</em>：空间复杂度<em>O</em>(1)，时间复杂度<em>O</em>(n)</p>
<p>如输入{1,3,5},{2,4,6}时，合并后的链表为{1,2,3,4,5,6}，所以对应的输出为{1,2,3,4,5,6}</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,3,5&#125;,&#123;2,4,6&#125;</span><br><span class="line">返回:  &#123;1,2,3,4,5,6&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;&#125;,&#123;&#125;</span><br><span class="line">返回:  &#123;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例3</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;-1,2,4&#125;,&#123;1,3,4&#125;</span><br><span class="line">返回:  &#123;-1,1,2,3,4,4&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>迭代</p>
</blockquote>
<p>设置 <strong><code>head</code></strong> 为虚拟节点，放置于新链表之前，最后返回的就是 <strong><code>head.next</code></strong> ；设置 <strong><code>cur</code></strong> 为当前节点，从 <strong><code>head</code></strong> 开始<br>当两个链表都非空时进入循环，令新链表的下一个节点 <strong><code>cur.next</code></strong> 为val更小的节点，相应的链表节点后移一位，每次循环记得 <strong><code>cur</code></strong> 也要后移一位。如果循环结束后还有链表非空， <strong><code>cur</code></strong> 指向非空链表，返回 <strong><code>head.next</code></strong></p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param pHead1 ListNode类 </span></span><br><span class="line"><span class="comment"> * @param pHead2 ListNode类 </span></span><br><span class="line"><span class="comment"> * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Merge</span><span class="params">( pHead1 *ListNode ,  pHead2 *ListNode )</span></span> *ListNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    head := &amp;ListNode&#123;&#125;</span><br><span class="line"></span><br><span class="line">    cur := head</span><br><span class="line">    <span class="keyword">for</span> pHead1 != <span class="literal">nil</span> &amp;&amp; pHead2 != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> pHead1.Val &lt; pHead2.Val &#123;</span><br><span class="line">            cur.Next = pHead1</span><br><span class="line">            pHead1 = pHead1.Next</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            cur.Next = pHead2</span><br><span class="line">            pHead2 = pHead2.Next</span><br><span class="line">        &#125;</span><br><span class="line">        cur = cur.Next</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> pHead1 != <span class="literal">nil</span> &#123;</span><br><span class="line">        cur.Next = pHead1</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> pHead2 != <span class="literal">nil</span> &#123;</span><br><span class="line">        cur.Next = pHead2</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> head.Next</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>合并二叉树</title>
    <url>//2023/05/16/%E7%AE%97%E6%B3%95/%E5%90%88%E5%B9%B6%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<p><strong>描述</strong><br>已知两颗二叉树，将它们合并成一颗二叉树。合并规则是：都存在的结点，就将结点值加起来，否则空的位置就由另一个树的结点来代替。</p>
<span id="more"></span>

<p>例如:<br>两颗二叉树如下:</p>
<ul>
<li>Tree 1<br><img src="/images/%E5%90%88%E5%B9%B6%E4%BA%8C%E5%8F%89%E6%A0%91-%E7%A4%BA%E4%BE%8B1.png"></li>
<li>Tree 2<br><img src="/images/%E5%90%88%E5%B9%B6%E4%BA%8C%E5%8F%89%E6%A0%91-%E7%A4%BA%E4%BE%8B2.png"></li>
<li>合并后的树<br><img src="/images/%E5%90%88%E5%B9%B6%E4%BA%8C%E5%8F%89%E6%A0%91-%E7%A4%BA%E4%BE%8B3.png"></li>
</ul>
<p><strong>题解</strong><br>要将一棵二叉树的节点与另一棵二叉树相加合并，肯定需要遍历两棵二叉树，那我们可以考虑同步遍历两棵二叉树，这样就可以将每次遍历到的值相加在一起。遍历的方式有多种，这里推荐前序递归遍历</p>
<ul>
<li>首先判断t1与t2是否为空，若为则用另一个代替，若都为空，返回的值也是空</li>
<li>然后依据前序遍历的特点，优先访问根节点，将两个根点的值相加创建到新树中</li>
<li>两棵树再依次同步进入左子树和右子树</li>
</ul>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param t1 TreeNode类 </span></span><br><span class="line"><span class="comment"> * @param t2 TreeNode类 </span></span><br><span class="line"><span class="comment"> * @return TreeNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">mergeTrees</span><span class="params">( t1 *TreeNode ,  t2 *TreeNode )</span></span> *TreeNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> t1 == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> t2</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> t2 == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> t1</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    t1.Val = t1.Val + t2.Val</span><br><span class="line">    t1.Left = mergeTrees(t1.Left, t2.Left)</span><br><span class="line">    t1.Right = mergeTrees(t1.Right, t2.Right)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> t1</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>对称的二叉树</title>
    <url>//2023/05/16/%E7%AE%97%E6%B3%95/%E5%AF%B9%E7%A7%B0%E7%9A%84%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一棵二叉树，判断其是否是自身的镜像（即：是否对称）</p>
<span id="more"></span>

<p>例如:</p>
<ul>
<li>对称二叉树<br><img src="/images/%E5%AF%B9%E7%A7%B0%E4%BA%8C%E5%8F%89%E6%A0%91-%E7%A4%BA%E4%BE%8B1.png"></li>
<li>非对称二叉树<br><img src="/images/%E5%AF%B9%E7%A7%B0%E4%BA%8C%E5%8F%89%E6%A0%91-%E7%A4%BA%E4%BE%8B2.png"></li>
</ul>
<p><strong>题解</strong></p>
<blockquote>
<p>二叉树递归</p>
</blockquote>
<ul>
<li><strong>终止条件：</strong> 当进入子问题的两个节点都为空，说明都到了叶子节点，且是同步的，因此结束本次子问题，返回true；当进入子问题的两个节点只有一个为空，或是元素值不相等，说明这里的对称不匹配，同样结束本次子问题，返回false。</li>
<li><strong>返回值：</strong> 每一级将子问题是否匹配的结果往上传递。</li>
<li><strong>本级任务：</strong> 每个子问题，需要按照上述思路，“根左右”走左边的时候“根右左”走右边，“根左右”走右边的时候“根右左”走左边，一起进入子问题，需要两边都是匹配才能对称。</li>
</ul>
<p><strong>具体做法</strong></p>
<ul>
<li>两种方向的前序遍历，同步过程中的当前两个节点，同为空，属于对称的范畴</li>
<li>当前两个节点只有一个为空或者节点值不相等，已经不是对称的二叉树了</li>
<li>第一个节点的左子树与第二个节点的右子树同步递归对比，第一个节点的右子树与第二个节点的左子树同步递归比较</li>
</ul>
<img src="/images/对称的二叉树.gif" alt="对称的二叉树">

<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param pRoot TreeNode类 </span></span><br><span class="line"><span class="comment"> * @return bool布尔型</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">isSymmetrical</span><span class="params">( pRoot *TreeNode )</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">return</span> isSame(pRoot, pRoot)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">isSame</span><span class="params">(left, right *TreeNode)</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> left == <span class="literal">nil</span> &amp;&amp; right == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (left == <span class="literal">nil</span> &amp;&amp; right != <span class="literal">nil</span>) || (left != <span class="literal">nil</span> &amp;&amp; right == <span class="literal">nil</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> left.Val == right.Val &amp;&amp; isSame(left.Left, right.Right) &amp;&amp; isSame(left.Right, right.Left)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>寻找峰值</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E5%AF%BB%E6%89%BE%E5%B3%B0%E5%80%BC/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一个长度为n的数组nums，请你找到峰值并返回其索引。数组可能包含多个峰值，在这种情况下，返回任何一个所在位置即可。</p>
<span id="more"></span>
<ol>
<li>峰值元素是指其值严格大于左右相邻值的元素。严格大于即不能有等于</li>
<li>假设<code>nums[-1]=nums[n]=−∞</code></li>
<li>对于所有有效的<code>i</code>都有<code>nums[i] != nums[i+1]</code></li>
</ol>
<p><em>数据范围：</em><br><strong>1≤nums.length≤2*10<sup>5</sup></strong><br><strong>-2<sup>31</sup>≤nums[i]≤2<sup>31</sup>-1</strong></p>
<p>如输入[2,4,1,2,7,8,4]时，会形成两个山峰，一个是索引为1，峰值为4的山峰，另一个是索引为5，峰值为8的山峰，如下图所示：<br><img src="/images/%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE-%E5%B3%B0%E5%80%BC-%E7%A4%BA%E4%BE%8B%E5%9B%BE.png"></p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [2,4,1,2,7,8,4]</span><br><span class="line">返回:  1</span><br><span class="line">说明:  4和8都是峰值元素，返回4的索引1或者8的索引5都可以 </span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [1,2,3,1]</span><br><span class="line">返回:  2</span><br><span class="line">说明:  3 是峰值元素，返回其索引 2</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<ul>
<li>二分查找首先从数组首尾开始，每次取中间值，直到首尾相遇</li>
<li>如果中间值的元素大于它右边的元素，说明往右是向下，我们不一定会遇到波峰，但是那就往左收缩区间</li>
<li>如果中间值大于右边的元素，说明此时往右是向上，向上一定能有波峰，那我们往右收缩区间</li>
<li>最后区间收尾相遇的点一定就是波峰</li>
</ul>
<img src="/images/寻找峰值.gif" alt="寻找峰值.gif">

<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param nums int整型一维数组 </span></span><br><span class="line"><span class="comment"> * @return int整型</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">findPeakElement</span><span class="params">( nums []<span class="type">int</span> )</span></span> <span class="type">int</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    left := <span class="number">0</span></span><br><span class="line">    rigth := <span class="built_in">len</span>(nums) <span class="number">-1</span></span><br><span class="line">    <span class="keyword">for</span> left &lt; rigth &#123;</span><br><span class="line">      middle := left + (rigth - left) / <span class="number">2</span></span><br><span class="line">      <span class="keyword">if</span> nums[middle] &gt; nums[middle+<span class="number">1</span>] &#123;</span><br><span class="line">        rigth = middle</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        left = middle + <span class="number">1</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> rigth</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二分查找</tag>
      </tags>
  </entry>
  <entry>
    <title>按之字形顺序打印二叉树</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E6%8C%89%E4%B9%8B%E5%AD%97%E5%BD%A2%E9%A1%BA%E5%BA%8F%E6%89%93%E5%8D%B0%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一个二叉树，返回该二叉树的之字形层序遍历，（第一层从左向右，下一层从右向左，一直这样交替）</p>
<span id="more"></span>

<p><strong>题解</strong></p>
<ul>
<li>首先判断二叉树是否为空，空树没有遍历结果</li>
<li>建立辅助队列，根节点首先进入队列。不管层次怎么访问，根节点一定是第一个，那它肯定排在队伍的最前面</li>
<li>每次进入一层，统计队列中元素的个数。因为每当访问完一层，下一层作为这一层的子节点，一定都加入队列，而再下一层还没有加入，因此此时队列中的元素个数就是这一层的元素个数</li>
<li>每次遍历这一层这么多的节点数，将其依次从队列中弹出，然后加入这一行的一维数组中，如果它们有子节点，依次加入队列排队等待访问</li>
<li>访问完这一层的元素后，将这个一维数组加入二维数组中，再访问下一层</li>
<li>二维数组中奇数下标(偶数层)数组反转</li>
</ul>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param pRoot TreeNode类 </span></span><br><span class="line"><span class="comment"> * @return int整型二维数组</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Print</span><span class="params">( pRoot *TreeNode )</span></span> [][]<span class="type">int</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> pRoot == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    queues := []*TreeNode&#123;pRoot&#125;</span><br><span class="line">    levels := [][]<span class="type">int</span>&#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> <span class="built_in">len</span>(queues) &gt; <span class="number">0</span> &#123;</span><br><span class="line">        n := <span class="built_in">len</span>(queues)</span><br><span class="line">        level := []<span class="type">int</span>&#123;&#125;</span><br><span class="line">        <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; n; i++ &#123;</span><br><span class="line">            root := queues[<span class="number">0</span>]</span><br><span class="line">            queues = queues[<span class="number">1</span>:]</span><br><span class="line">            level = <span class="built_in">append</span>(level, root.Val)</span><br><span class="line">            <span class="keyword">if</span> root.Left != <span class="literal">nil</span> &#123;</span><br><span class="line">                queues = <span class="built_in">append</span>(queues, root.Left)</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> root.Right != <span class="literal">nil</span> &#123;</span><br><span class="line">                queues = <span class="built_in">append</span>(queues, root.Right)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        k1 := <span class="built_in">len</span>(level)</span><br><span class="line">        k2 := <span class="built_in">len</span>(levels)</span><br><span class="line">        <span class="comment">// 当前层为偶数层,从右到左遍历.(逆转数组level)</span></span><br><span class="line">        <span class="keyword">if</span> k2 % <span class="number">2</span> == <span class="number">1</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> index := <span class="number">0</span>; index &lt; k1/<span class="number">2</span>; index++ &#123;</span><br><span class="line">                level[index], level[k1-index<span class="number">-1</span>] = level[k1-index<span class="number">-1</span>], level[index]</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        levels = <span class="built_in">append</span>(levels, level)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> levels</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>数组中的逆序对</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E6%95%B0%E7%BB%84%E4%B8%AD%E7%9A%84%E9%80%86%E5%BA%8F%E5%AF%B9/</url>
    <content><![CDATA[<p><strong>描述</strong><br>在数组中的两个数字，如果前面一个数字大于后面的数字，则这两个数字组成一个逆序对。输入一个数组,求出这个数组中的逆序对的总数P。并将P对1000000007取模的结果输出。 即输出P mod 1000000007</p>
<span id="more"></span>

<p><em>数据范围：</em></p>
<ul>
<li>对于50%的数据， <strong>size≤10<sup>4</sup></strong></li>
<li>对于100%的数据， <strong>size≤10<sup>5</sup></strong></li>
<li>数组中所有数字的值满足 <strong>0≤|val|≤10<sup>9</sup></strong></li>
</ul>
<p><em>要求：</em> 空间复杂度<em>O</em>(n)，时间复杂度<em>O</em>(n<em>log</em>n)</p>
<p><strong>输入描述</strong><br>题目保证输入的数组中没有的相同的数字</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [1,2,3,4,5,6,7,0]</span><br><span class="line">返回:  7</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [1,2,3]</span><br><span class="line">返回:  0</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>归并排序、递归</p>
</blockquote>
<p><strong>先分:</strong> 将数组分为两个子数组，两个子数组分为四个子数组，依次向下分，直到数组不能再分为止<br><strong>合并:</strong> 就是从最小的数组按照顺序合并，从小到大或从大到小，依次向上合并，最后得到合并完的顺序数组</p>
<p><font color='red'><strong>Tips:</strong></font> 在合并数组的时候，当发现右边的小于左边的时候，此时可以直接求出当前产生的逆序对的个数<br><img src="/imags/%E6%95%B0%E7%BB%84%E4%B8%AD%E7%9A%84%E9%80%86%E5%BA%8F%E5%AF%B9.png"></p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param data int整型一维数组</span></span><br><span class="line"><span class="comment"> * @return int整型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">InversePairs</span><span class="params">(data []<span class="type">int</span>)</span></span> <span class="type">int</span> &#123;</span><br><span class="line">	<span class="comment">// write code here</span></span><br><span class="line">	tmp := <span class="built_in">make</span>([]<span class="type">int</span>, <span class="built_in">len</span>(data))</span><br><span class="line">	count := <span class="number">0</span></span><br><span class="line">	mergeSort(data, tmp, <span class="number">0</span>, <span class="built_in">len</span>(data)<span class="number">-1</span>, &amp;count)</span><br><span class="line">	<span class="keyword">return</span> count</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">mergeSort</span><span class="params">(data, tmp []<span class="type">int</span>, start, end <span class="type">int</span>, count *<span class="type">int</span>)</span></span> &#123;</span><br><span class="line">	<span class="keyword">if</span> start &gt;= end &#123;</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	mid := start + (end-start)&gt;&gt;<span class="number">1</span></span><br><span class="line"></span><br><span class="line">	mergeSort(data, tmp, start, mid, count)</span><br><span class="line">	mergeSort(data, tmp, mid+<span class="number">1</span>, end, count)</span><br><span class="line">	merge(data, tmp, start, mid, end, count)</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">merge</span><span class="params">(data, tmp []<span class="type">int</span>, start, mid, end <span class="type">int</span>, count *<span class="type">int</span>)</span></span> &#123;</span><br><span class="line">	<span class="keyword">if</span> start &gt;= end &#123;</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	i := start</span><br><span class="line">	j := mid + <span class="number">1</span></span><br><span class="line">	k := start</span><br><span class="line">	<span class="keyword">for</span> i &lt;= mid &amp;&amp; j &lt;= end &#123;</span><br><span class="line">		<span class="keyword">if</span> data[i] &lt;= data[j] &#123;</span><br><span class="line">			tmp[k] = data[i]</span><br><span class="line">			i++</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			*count = *count + (mid - i + <span class="number">1</span>)</span><br><span class="line">			*count = *count % <span class="number">1000000007</span></span><br><span class="line">			tmp[k] = data[j]</span><br><span class="line">			j++</span><br><span class="line">		&#125;</span><br><span class="line">		k++</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> i &lt;= mid &#123;</span><br><span class="line">		tmp[k] = data[i]</span><br><span class="line">		i++</span><br><span class="line">		k++</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> j &lt;= end &#123;</span><br><span class="line">		tmp[k] = data[j]</span><br><span class="line">		j++</span><br><span class="line">		k++</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> i = start; i &lt;= end; i++ &#123;</span><br><span class="line">		data[i] = tmp[i]</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二分查找</tag>
      </tags>
  </entry>
  <entry>
    <title>旋转数组的最小数字</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E6%97%8B%E8%BD%AC%E6%95%B0%E7%BB%84%E7%9A%84%E6%9C%80%E5%B0%8F%E6%95%B0%E5%AD%97/</url>
    <content><![CDATA[<p><strong>描述</strong><br>有一个长度为 n 的非降序数组，比如[1,2,3,4,5]，将它进行旋转，即把一个数组最开始的若干个元素搬到数组的末尾，变成一个旋转数组，比如变成了[3,4,5,1,2]，或者[4,5,1,2,3]这样的。请问，给定这样一个旋转数组，求数组中的最小值。</p>
<span id="more"></span>
<p><em>数据范围：</em><code>1≤n≤10000</code>，数组中任意元素的值<code>0≤val≤10000</code><br><em>要求：</em>空间复杂度<em>O</em>(1)，时间复杂度<em>O</em>(<em>log</em>n)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [3,4,5,1,2]</span><br><span class="line">返回:  1</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [3,100,200,3]</span><br><span class="line">返回:  3</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<ul>
<li>数组可以分为两个有序的子数组。其中，左排序的数组的值大于右排序数组中的值<br><img src="/images/%E6%97%8B%E8%BD%AC%E6%95%B0%E7%BB%84%E7%9A%84%E6%9C%80%E5%B0%8F%E6%95%B0%E5%AD%971.png"></li>
<li>声明left,right 分别指向数组的左右两端</li>
<li>mid &#x3D; (left+right) &#x2F; 2 为二分的中间位置</li>
<li>mid，left,right分为三种情况<ul>
<li><code>rotateArray[mid] &gt; rotateArray[right]</code>时， 那么 最小值一定在 [mid+1,right]区间中</li>
<li><code>rotateArray[mid] &lt; rotateArray[right]</code>时，那么最小值一定在[left,mid]区间内</li>
<li><code>rotateArray[mid] = rotateArray[right]</code>时，无法判断最小值在哪个区间，所以此时只能缩小right的值<br><img src="/images/%E6%97%8B%E8%BD%AC%E6%95%B0%E7%BB%84%E7%9A%84%E6%9C%80%E5%B0%8F%E6%95%B0%E5%AD%972.png"></li>
</ul>
</li>
</ul>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param rotateArray int整型一维数组 </span></span><br><span class="line"><span class="comment"> * @return int整型</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">minNumberInRotateArray</span><span class="params">( rotateArray []<span class="type">int</span> )</span></span> <span class="type">int</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    left, right := <span class="number">0</span>, <span class="built_in">len</span>(rotateArray) - <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> left &lt; right &#123;</span><br><span class="line">        mid :=  left + (right - left)&gt;&gt;<span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> rotateArray[mid] &lt; rotateArray[right] &#123;</span><br><span class="line">            right = mid</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> rotateArray[mid] &gt; rotateArray[right] &#123;</span><br><span class="line">            left = mid + <span class="number">1</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            right -= <span class="number">1</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> rotateArray[left]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二分查找</tag>
      </tags>
  </entry>
  <entry>
    <title>比较版本号</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E6%AF%94%E8%BE%83%E7%89%88%E6%9C%AC%E5%8F%B7/</url>
    <content><![CDATA[<p><strong>描述</strong><br>项目发布项目版本时会有版本号，比如1.02.11，2.14.4等等。现在给你2个版本号version1和version2，请你比较他们的大小</p>
<span id="more"></span>

<p>版本号是由修订号组成，修订号与修订号之间由一个”.”连接。1个修订号可能有多位数字组成，修订号可能包含前导0，且是合法的。例如，1.02.11，0.1，0.2都是合法的版本号。每个版本号至少包含1个修订号。修订号从左到右编号，下标从0开始，最左边的修订号下标为0，下一个修订号下标为1，以此类推。</p>
<p><em>比较规则：</em></p>
<ul>
<li>比较版本号时，请按从左到右的顺序依次比较它们的修订号。比较修订号时，只需比较忽略任何前导零后的整数值。比如”0.1”和”0.01”的版本号是相等的</li>
<li>如果版本号没有指定某个下标处的修订号，则该修订号视为0。例如，”1.1”的版本号小于”1.1.1”。因为”1.1”的版本号相当于”1.1.0”，第3位修订号的下标为0，小于1</li>
<li>version1 &gt; version2 返回1，如果 version1 &lt; version2 返回-1，不然返回0.</li>
</ul>
<p><em>数据范围：</em><code>1≤version1.length,version2.length≤1000</code><br>version1 和 version2 的修订号不会超过int的表达范围，即不超过 <strong>32 位整数</strong> 的范围</p>
<p><em>要求：</em> 空间复杂度<em>O</em>(1)，时间复杂度<em>O</em>(n)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &quot;1.1&quot;,&quot;2.1&quot;</span><br><span class="line">返回:  -1</span><br><span class="line">说明:  version1 中下标为 0 的修订号是 &quot;1&quot;，version2 中下标为 0 的修订号是 &quot;2&quot; 。1 &lt; 2，所以 version1 &lt; version2，返回-1</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &quot;1.1&quot;,&quot;1.01&quot;</span><br><span class="line">返回:  0</span><br><span class="line">说明:  version2忽略前导0，为&quot;1.1&quot;，和version相同，返回0</span><br></pre></td></tr></table></figure>

<p><strong>示例3</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &quot;1.1&quot;,&quot;1.1.1&quot;</span><br><span class="line">返回:  -1</span><br><span class="line">说明:  &quot;1.1&quot;的版本号小于&quot;1.1.1&quot;。因为&quot;1.1&quot;的版本号相当于&quot;1.1.0&quot;，第3位修订号的下标为0，小于1，所以version1 &lt; version2，返回-1 </span><br></pre></td></tr></table></figure>

<p><strong>示例4</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &quot;2.0.1&quot;,&quot;2&quot;</span><br><span class="line">返回:  1</span><br><span class="line">说明:  version1的下标2&gt;version2的下标2，返回1 </span><br></pre></td></tr></table></figure>

<p><strong>示例5</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &quot;0.226&quot;,&quot;0.36&quot;</span><br><span class="line">返回:  1</span><br><span class="line">说明:  226&gt;36，version1的下标2&gt;version2的下标2，返回1</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<p>既然是比较两个字符串每个点之间的数字是否相同，就直接将两个字符串分割成数组，比较相同下标时的元素大小即可</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;strconv&quot;</span></span><br><span class="line">	<span class="string">&quot;strings&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 比较版本号</span></span><br><span class="line"><span class="comment"> * @param version1 string字符串</span></span><br><span class="line"><span class="comment"> * @param version2 string字符串</span></span><br><span class="line"><span class="comment"> * @return int整型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">compare</span><span class="params">(version1 <span class="type">string</span>, version2 <span class="type">string</span>)</span></span> <span class="type">int</span> &#123;</span><br><span class="line">	<span class="comment">// write code here</span></span><br><span class="line">	v1 := strings.Split(version1, <span class="string">&quot;.&quot;</span>)</span><br><span class="line">	v2 := strings.Split(version2, <span class="string">&quot;.&quot;</span>)</span><br><span class="line">	<span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="built_in">len</span>(v1) || i &lt; <span class="built_in">len</span>(v2); i++ &#123;</span><br><span class="line">		x, y := <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">		<span class="keyword">if</span> i &lt; <span class="built_in">len</span>(v1) &#123;</span><br><span class="line">			x, _ = strconv.Atoi(v1[i])</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> i &lt; <span class="built_in">len</span>(v2) &#123;</span><br><span class="line">			y, _ = strconv.Atoi(v2[i])</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> x &gt; y &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> x &lt; y &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二分查找</tag>
      </tags>
  </entry>
  <entry>
    <title>链表中倒数最后K个节点</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E9%93%BE%E8%A1%A8%E4%B8%AD%E5%80%92%E6%95%B0%E6%9C%80%E5%90%8EK%E4%B8%AA%E8%8A%82%E7%82%B9/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>输入一个长度为 n 的链表，设链表中的元素的值为 a<sub>i</sub> ，返回该链表中倒数第k个节点。<br>如果该链表长度小于k，请返回一个长度为 0 的链表。</p>
<span id="more"></span>

<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,2,3,4,5&#125;,2</span><br><span class="line">返回:  &#123;4,5&#125;</span><br><span class="line">说明:  返回倒数第2个节点4，系统会打印后面所有的节点来比较。 </span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;2&#125;,8</span><br><span class="line">返回:  &#123;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>快慢指针</p>
</blockquote>
<p>第一个指针先移动k步，然后第二个指针再从头开始，这个时候这两个指针同时移动，当第一个指针到链表的末尾的时候，返回第二个指针即可</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param pHead ListNode类</span></span><br><span class="line"><span class="comment"> * @param k int整型</span></span><br><span class="line"><span class="comment"> * @return ListNode类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">FindKthToTail</span><span class="params">(pHead *ListNode, k <span class="type">int</span>)</span></span> *ListNode &#123;</span><br><span class="line">	<span class="comment">// write code here</span></span><br><span class="line">	<span class="keyword">if</span> pHead == <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	fast := pHead</span><br><span class="line">	slow := pHead</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> index := <span class="number">0</span>; index &lt; k; index++ &#123;</span><br><span class="line">		<span class="keyword">if</span> fast == <span class="literal">nil</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> fast</span><br><span class="line">		&#125;</span><br><span class="line">		fast = fast.Next</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> fast != <span class="literal">nil</span> &#123;</span><br><span class="line">		fast = fast.Next</span><br><span class="line">		slow = slow.Next</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> slow</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>链表中环的入口点</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E9%93%BE%E8%A1%A8%E4%B8%AD%E7%8E%AF%E7%9A%84%E5%85%A5%E5%8F%A3%E7%82%B9/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>给一个长度为n链表，若其中包含环，请找出该链表的环的入口结点，否则，返回null。</p>
<span id="more"></span>

<p><strong>输入描述：</strong></p>
<p>输入分为2段，第一段是入环前的链表部分，第二段是链表环的部分，后台会根据第二段是否为空将这两段组装成一个无环或者有环单链表</p>
<p><strong>返回值描述：</strong></p>
<p>返回链表的环的入口结点即可，我们后台程序会打印这个结点对应的结点值；若没有，则返回对应编程语言的空结点即可。</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,2&#125;,&#123;3,4,5&#125;</span><br><span class="line">返回:  3</span><br><span class="line">说明:  返回环形链表入口结点，我们后台程序会打印该环形链表入口结点对应的结点值，即3 </span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1&#125;,&#123;&#125;</span><br><span class="line">返回:  &quot;null&quot;</span><br><span class="line">说明:  没有环，返回对应编程语言的空结点，后台程序会打印&quot;null&quot;</span><br></pre></td></tr></table></figure>

<p><strong>示例3</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;&#125;,&#123;2&#125;</span><br><span class="line">返回:  2</span><br><span class="line">说明:  环的部分只有一个结点，所以返回该环形链表入口结点，后台程序打印该结点对应的结点值，即2</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>快慢指针</p>
</blockquote>
<p>通过定义slow和fast指针，slow每走一步，fast走两步，若是有环，则一定会在环的某个结点处相遇（slow &#x3D;&#x3D; fast）。</p>
<p>从相遇处到入口结点的距离与头结点与入口结点的距离相同。</p>
<p><img src="https://uploadfiles.nowcoder.com/images/20210717/973747578_1626453132845/80C078E92E276EB70274AC2CF5AD2130" alt="环的入口"></p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">EntryNodeOfLoop</span><span class="params">(pHead *ListNode)</span></span> *ListNode&#123;</span><br><span class="line">    fast := pHead</span><br><span class="line">    slow := pHead</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> fast != <span class="literal">nil</span> &amp;&amp; fast.Next != <span class="literal">nil</span> &#123;</span><br><span class="line">        fast = fast.Next.Next</span><br><span class="line">        slow = slow.Next</span><br><span class="line">        <span class="keyword">if</span> fast == slow &#123;</span><br><span class="line">            fast = pHead</span><br><span class="line">            <span class="keyword">for</span> fast != slow &#123;</span><br><span class="line">                fast = fast.Next</span><br><span class="line">                slow = slow.Next</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> slow</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>链表中的节点每k个一组翻转</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E9%93%BE%E8%A1%A8%E4%B8%AD%E7%9A%84%E8%8A%82%E7%82%B9%E6%AF%8Fk%E4%B8%AA%E4%B8%80%E7%BB%84%E7%BF%BB%E8%BD%AC/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>将给出的链表中的节点每 k 个一组翻转，返回翻转后的链表。如果链表中的节点数不是 k 的倍数，将最后剩下的节点保持原样。你不能更改节点中的值，只能更改节点本身。</p>
<span id="more"></span>

<p><em>数据范围</em>： <strong><code>0≤n≤2000</code></strong> ， <strong><code>1≤k≤2000</code></strong> ，链表中每个元素都满足 <strong><code>0≤val≤1000</code></strong></p>
<p>要求空间复杂度<em>O</em>(1)，时间复杂度<em>O</em>(n)</p>
<p>例如:</p>
<p>给定的链表 <strong><code>1→2→3→4→5</code></strong></p>
<p>对于k&#x3D;2，返回 <strong><code>2→1→4→3→5</code></strong></p>
<p>对于k&#x3D;3，返回 <strong><code>3→2→1→4→5</code></strong></p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,2,3,4,5&#125;,2</span><br><span class="line">返回:  &#123;2,1,4,3,5&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;&#125;,1</span><br><span class="line">返回:  &#123;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>递归、虚拟头节点</p>
<p><strong>终止条件：</strong> 当进行到最后一个分组，即不足k次遍历到链表尾（0次也算），就将剩余的部分直接返回<br><strong>返回值：</strong> 每一级要返回的就是翻转后的这一分组的头，以及连接好它后面所有翻转好的分组链表<br><strong>本级任务：</strong> 对于每个子问题，先遍历k次，找到该组结尾在哪里，然后从这一组开头遍历到结尾，依次翻转，结尾就可以作为下一个分组的开头，而先前指向开头的元素已经跑到了这一分组的最后，可以用它来连接它后面的子问题，即后面分组的头</p>
</blockquote>
<ol>
<li>计算当前链表长度，若长度小于k，不用反转直接返回头节点</li>
<li>反转前k个节点</li>
<li>如果第k+1个节点存在，则从第k+1个节点开始采用递归方法</li>
</ol>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * </span></span><br><span class="line"><span class="comment">  * @param head ListNode类 </span></span><br><span class="line"><span class="comment">  * @param k int整型 </span></span><br><span class="line"><span class="comment">  * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">reverseKGroup</span><span class="params">( head *ListNode ,  k <span class="type">int</span> )</span></span> *ListNode &#123;</span><br><span class="line">    length := size(head)</span><br><span class="line">    <span class="keyword">if</span> head == <span class="literal">nil</span> || head.Next == <span class="literal">nil</span> || length &lt; k &#123;</span><br><span class="line">        <span class="keyword">return</span> head</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    dummy := &amp;ListNode&#123;Next: head&#125;</span><br><span class="line">    <span class="keyword">for</span> index := <span class="number">1</span>; index &lt; k; index++ &#123;</span><br><span class="line">        temp := head.Next</span><br><span class="line">        head.Next = temp.Next</span><br><span class="line">        temp.Next = dummy.Next</span><br><span class="line">        dummy.Next = temp</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> head != <span class="literal">nil</span> &#123;</span><br><span class="line">        head.Next = reverseKGroup(head.Next, k)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> dummy.Next</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">size</span><span class="params">(head *ListNode)</span></span> <span class="type">int</span> &#123;</span><br><span class="line">    size := <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> head != <span class="literal">nil</span> &#123;</span><br><span class="line">        size++</span><br><span class="line">        head = head.Next</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> size</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>链表内指定区域反转</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E9%93%BE%E8%A1%A8%E5%86%85%E6%8C%87%E5%AE%9A%E5%8C%BA%E5%9F%9F%E5%8F%8D%E8%BD%AC/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>将一个节点数为 size 链表 m 位置到 n 位置之间的区间反转，要求时间复杂度 <em>O</em>(n)，空间复杂度 <em>O</em>(1)。</p>
<span id="more"></span>
<p>例如:</p>
<p>给出的链表为 <strong><code>1→2→3→4→5→NULL, m=2,n=4</code></strong><br>返回 <strong><code>1→4→3→2→5→NULL</code></strong></p>
<p><em>数据范围</em>： 链表长度 <strong><code>0&lt;size≤1000</code></strong> ， <strong><code>0&lt;m≤n≤size</code></strong> ，链表中每个节点的值满足 <strong><code>∣val∣≤1000</code></strong></p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入: &#123;1,2,3,4,5&#125;,2,4</span><br><span class="line">返回: &#123;1,4,3,2,5&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入: &#123;5&#125;,1,1</span><br><span class="line">返回: &#123;5&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>双指针、两次遍历</p>
</blockquote>
<blockquote>
<p>要反转局部链表,可以将该局部部分当作完整链表进行反转</p>
<p>再将已经反转好的链表与其他节点建立连接,重构链表</p>
</blockquote>
<ol>
<li>我们可以在链表前加一个表头，后续返回时去掉就好了，因为如果要从链表头的位置开始反转，在多了一个表头的情况下就能保证第一个节点永远不会反转，不会到后面去</li>
<li>使用两个指针，一个指向当前节点，一个指向前序节点</li>
<li>依次遍历链表，到第m个的位置</li>
<li>对于从m到n这些个位置的节点，依次断掉指向后续的指针，反转指针方向</li>
<li>返回时去掉我们添加的表头</li>
</ol>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * </span></span><br><span class="line"><span class="comment">  * @param head ListNode类 </span></span><br><span class="line"><span class="comment">  * @param m int整型 </span></span><br><span class="line"><span class="comment">  * @param n int整型 </span></span><br><span class="line"><span class="comment">  * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">reverseBetween</span><span class="params">( head *ListNode ,  m <span class="type">int</span> ,  n <span class="type">int</span> )</span></span> *ListNode &#123;</span><br><span class="line">    dummy := &amp;ListNode&#123;Next: head&#125;</span><br><span class="line">    pre := dummy</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> index := <span class="number">1</span>; index &lt; m; index++ &#123;</span><br><span class="line">        pre = pre.Next</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cur := pre.Next</span><br><span class="line">    <span class="keyword">for</span> index := m; index &lt; n; index++ &#123;</span><br><span class="line">        temp := cur.Next</span><br><span class="line">        cur.Next = temp.Next</span><br><span class="line">        temp.Next = pre.Next</span><br><span class="line">        pre.Next = temp</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> dummy.Next</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>链表的奇偶重排</title>
    <url>//2023/05/14/%E7%AE%97%E6%B3%95/%E9%93%BE%E8%A1%A8%E7%9A%84%E5%A5%87%E5%81%B6%E9%87%8D%E6%8E%92/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一个单链表，请设定一个函数，将链表的奇数位节点和偶数位节点分别放在一起，重排后输出。<br>注意是节点的编号而非节点的数值。</p>
<span id="more"></span>

<p><em>数据范围：</em> 节点数量满足 <strong>0≤n≤10<sup>5</sup></strong> ,节点中的值都满足 <strong>0≤val≤1000</strong><br><em>要求：</em> 空间复杂度<em>O</em>(n),时间复杂度<em>O</em>(n)</p>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  &#123;1,4,6,3,7&#125;</span><br><span class="line">返回:  &#123;1,6,7,4,3&#125;</span><br><span class="line">说明:  1-&gt;4-&gt;6-&gt;3-&gt;7-&gt;NULL</span><br><span class="line">       重排后为</span><br><span class="line">       1-&gt;6-&gt;7-&gt;4-&gt;3-&gt;NULL</span><br><span class="line">       奇数位节点有1,6,7，偶数位节点有4,3。重排后为1,6,7,4,3</span><br></pre></td></tr></table></figure>

<p><strong>备注</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">链表长度不大于200000。每个数范围均在int内。</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>双指针,分别指向第一个节点(奇节点)和第二个节点(偶节点)</p>
</blockquote>
<ul>
<li>先保存奇数头节点和偶数头节点（避免引用中的属性发生变化）</li>
<li>一个引用指向奇数节点，一个引用指向偶数节点，开始往后处理结点指向</li>
<li>结束的条件是，偶数节点不为null且偶数节点的next不为null</li>
<li>最后将偶数头节点挂到奇数链表尾节点</li>
</ul>
<p><img src="/images/%E9%93%BE%E8%A1%A8%E7%9A%84%E5%A5%87%E5%81%B6%E9%87%8D%E6%8E%92.png"></p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param head ListNode类 </span></span><br><span class="line"><span class="comment"> * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">oddEvenList</span><span class="params">( head *ListNode )</span></span> *ListNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> head == <span class="literal">nil</span> || head.Next == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> head</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 奇数链表头</span></span><br><span class="line">    odd := head</span><br><span class="line">    oddHead := odd</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 偶数链表头</span></span><br><span class="line">    even := head.Next</span><br><span class="line">    evenHead := even</span><br><span class="line"></span><br><span class="line">    node := head.Next.Next</span><br><span class="line">    <span class="keyword">for</span> node != <span class="literal">nil</span> &#123;</span><br><span class="line">        odd.Next = node</span><br><span class="line">        odd = odd.Next</span><br><span class="line">        node = node.Next</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> node != <span class="literal">nil</span> &#123;</span><br><span class="line">            even.Next = node</span><br><span class="line">            even = even.Next</span><br><span class="line">            node = node.Next</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    even.Next = node</span><br><span class="line">    odd.Next = evenHead</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> oddHead</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>链表相加</title>
    <url>//2023/05/13/%E7%AE%97%E6%B3%95/%E9%93%BE%E8%A1%A8%E7%9B%B8%E5%8A%A0/</url>
    <content><![CDATA[<p><strong>描述</strong></p>
<p>假设链表中每一个节点的值都在 0 - 9 之间，那么链表整体就可以代表一个整数。给定两个这种链表，请生成代表两个整数相加值的结果链表。</p>
<span id="more"></span>
<p><strong>示例1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [9,3,7],[6,3]</span><br><span class="line">返回:  &#123;1,0,0,0&#125;</span><br></pre></td></tr></table></figure>

<p><strong>示例2</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入:  [0],[6,3]</span><br><span class="line">返回:  &#123;6,3&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解</strong></p>
<blockquote>
<p>链表反转</p>
</blockquote>
<ol>
<li>两个链表反转</li>
<li>遍历两个链表，将相加结果接入结果链表</li>
<li>反转结果链表</li>
</ol>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ListNode <span class="keyword">struct</span>&#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Next *ListNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param head1 ListNode类 </span></span><br><span class="line"><span class="comment"> * @param head2 ListNode类 </span></span><br><span class="line"><span class="comment"> * @return ListNode类</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">addInList</span><span class="params">( head1 *ListNode ,  head2 *ListNode )</span></span> *ListNode &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    rl1, rl2 := reverse(head1), reverse(head2)</span><br><span class="line">    newHead := &amp;ListNode&#123;&#125;</span><br><span class="line">    cur := newHead</span><br><span class="line">    plus := <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> rl1 != <span class="literal">nil</span> || rl2 != <span class="literal">nil</span> &#123;</span><br><span class="line">        node := &amp;ListNode&#123;Val: plus&#125;</span><br><span class="line">        <span class="keyword">if</span> rl1 != <span class="literal">nil</span> &#123;</span><br><span class="line">            node.Val += rl1.Val</span><br><span class="line">            rl1 = rl1.Next</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> rl2 != <span class="literal">nil</span> &#123;</span><br><span class="line">            node.Val += rl2.Val</span><br><span class="line">            rl2 = rl2.Next</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        plus = node.Val / <span class="number">10</span></span><br><span class="line">        node.Val = node.Val % <span class="number">10</span></span><br><span class="line">        cur.Next = node</span><br><span class="line">        cur = cur.Next</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> plus &gt; <span class="number">0</span> &#123;</span><br><span class="line">        cur.Next = &amp;ListNode&#123;Val: plus&#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> reverse(newHead.Next)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">reverse</span><span class="params">(head *ListNode)</span></span> *ListNode &#123;</span><br><span class="line">    <span class="keyword">if</span> head == <span class="literal">nil</span> || head.Next == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> head</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> pre *ListNode</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> head != <span class="literal">nil</span> &#123;</span><br><span class="line">        temp := head.Next</span><br><span class="line">        head.Next = pre</span><br><span class="line">        pre = head</span><br><span class="line">        head = temp</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> pre</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>Kubernetes调度器</title>
    <url>//2023/05/23/k8s/%E8%B0%83%E5%BA%A6%E5%99%A8/</url>
    <content><![CDATA[<p>在kubernetes中， <strong>调度</strong> 是指将Pod放置到合适的节点，以便对应节点的kubelet能够进行这些Pod。</p>
<h2 id="概览"><a href="#概览" class="headerlink" title="概览"></a>概览</h2><p>调度器通过kubernetes的检测(Watch)机制来发现集群中新创建且尚未调度到节点的Pod。调度器会将发现的每一个未调度的Pod调度到一个合适的节点上来运行。</p>
<span id="more"></span>

<h2 id="调度选择"><a href="#调度选择" class="headerlink" title="调度选择"></a>调度选择</h2><h3 id="过滤阶段"><a href="#过滤阶段" class="headerlink" title="过滤阶段"></a>过滤阶段</h3><p>过滤阶段会将所有满足 Pod 调度需求的节点选出来。 例如，PodFitsResources 过滤函数会检查候选节点的可用资源能否满足 Pod 的资源请求。 在过滤之后，得出一个节点列表，里面包含了所有可调度节点；通常情况下， 这个节点列表包含不止一个节点。如果这个列表是空的，代表这个 Pod 不可调度</p>
<h3 id="打分阶段"><a href="#打分阶段" class="headerlink" title="打分阶段"></a>打分阶段</h3><p>在打分阶段，调度器会为 Pod 从所有可调度节点中选取一个最合适的节点。 根据当前启用的打分规则，调度器会给每一个可调度节点进行打分</p>
]]></content>
      <tags>
        <tag>kubernetes</tag>
        <tag>调度器</tag>
      </tags>
  </entry>
  <entry>
    <title>判断是不是二叉搜索树</title>
    <url>//2023/06/01/%E7%AE%97%E6%B3%95/%E5%88%A4%E6%96%AD%E6%98%AF%E4%B8%8D%E6%98%AF%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91/</url>
    <content><![CDATA[<p><strong>描述</strong><br>给定一个二叉树根节点，请你判断这棵树是不是二叉搜索树。</p>
<span id="more"></span>

<blockquote>
<p>二叉搜索树满足每个节点的左子树上的所有节点均小于当前节点且右子树上的所有节点均大于当前节点。</p>
</blockquote>
<p><strong>题解</strong><br>二叉搜索树的特性就是中序遍历是递增序。既然是判断是否是二叉搜索树，那我们可以使用中序递归遍历。只要之前的节点是二叉树搜索树，那么如果当前的节点小于上一个节点值那么就可以向下判断</p>
<ul>
<li>前置节点默认是 <code>-1&lt;&lt;(32-1)</code></li>
<li>首先递归到最左</li>
<li>往后遍历整棵树，依次连接pre与当前节点，并更新pre</li>
<li>左子树如果不是二叉搜索树返回false</li>
<li>判断当前节点是不是小于前置节点，更新前置节点</li>
<li>递归右子树</li>
</ul>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> TreeNode <span class="keyword">struct</span> &#123;</span><br><span class="line">    Val <span class="type">int</span></span><br><span class="line">    Left *TreeNode</span><br><span class="line">    Right *TreeNode</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param root TreeNode类</span></span><br><span class="line"><span class="comment"> * @return bool布尔型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">var</span> pre = <span class="number">-1</span> &lt;&lt; (<span class="number">32</span> - <span class="number">1</span>)</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">isValidBST</span><span class="params">( root *TreeNode )</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">    <span class="comment">// write code here</span></span><br><span class="line">    <span class="keyword">if</span> root == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> !isValidBST(root.Left) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (root.Val &lt; pre) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    pre = root.Val</span><br><span class="line">    <span class="keyword">return</span> isValidBST(root.Right)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>算法</tag>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>Golang垃圾回收机制</title>
    <url>//2023/06/16/%E7%BC%96%E7%A8%8B%E8%AF%AD%E8%A8%80/Golang%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[]]></content>
      <tags>
        <tag>Go</tag>
        <tag>垃圾回收机制</tag>
      </tags>
  </entry>
  <entry>
    <title>Python垃圾回收机制</title>
    <url>//2023/06/15/%E7%BC%96%E7%A8%8B%E8%AF%AD%E8%A8%80/Python%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>Python的垃圾回收机制采用以<strong>引用计数</strong>为主，以<strong>标记-清除</strong>和<strong>分代收集</strong>为辅的策略</p>
<span id="more"></span>

<h1 id="引用计数"><a href="#引用计数" class="headerlink" title="引用计数"></a>引用计数</h1><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>每个对象有一个整型的引用计数属性<code>ob_refcnt</code>，用于记录对象被引用的次数。每当新的引用指向该对象时，它的引用计数<code>ob_refcnt</code>加一；每当该对象的引用失效时，<br>他的引用计数<code>ob_refcnt</code>减一。一旦对象的引用计数为<code>0</code>，该对象立即被回收，对象占用的内存空间将被释放。</p>
<p>可以使用<code>sys.getrefcount</code>函数检查该对象的引用计数。（PS: 该函数获取的引用计数总是多<code>1</code>, 因为该函数在调用时也有对该对象的引用）<br><img src="/images/Python%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6-%E5%BC%95%E7%94%A8%E8%AE%A1%E6%95%B0.png"></p>
<h2 id="计数增减条件"><a href="#计数增减条件" class="headerlink" title="计数增减条件"></a>计数增减条件</h2><h3 id="引数加一条件"><a href="#引数加一条件" class="headerlink" title="引数加一条件"></a>引数加一条件</h3><ul>
<li>对象被创建</li>
<li>对象被引用</li>
<li>对象作为函数的参数</li>
<li>对象作为容器的元素</li>
</ul>
<h3 id="引数减一条件"><a href="#引数减一条件" class="headerlink" title="引数减一条件"></a>引数减一条件</h3><ul>
<li>对象被显式销毁。例如: <code>del y</code></li>
<li>变量重新赋予新的对象</li>
<li>对象离开它的作用域，常见场景: 变量作为函数的参数。函数执行完成后该对象引用计数减一</li>
<li>对象所在的容器被销毁，或从容器中删除对象</li>
</ul>
<h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul>
<li>高效、逻辑简单，只需根据规则对计数器做加减法</li>
<li>实时性。一旦对象的计数器为零，就说明对象永远不可能再被用到，无须等待特定时机，直接释放内存。</li>
</ul>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>需要为对象分配引用计数空间，增大了内存消耗</li>
<li>嵌套对象需要循环调用，耗时长。如字典对象</li>
<li>循环引用 &#x3D;&gt; 计数器永不为<code>0</code>, 对象无法回收，导致内存泄漏</li>
</ul>
<h1 id="标记-清除"><a href="#标记-清除" class="headerlink" title="标记-清除"></a>标记-清除</h1><blockquote>
<p>算法是一种基于追踪回收（tracing GC）技术实现的垃圾回收算法</p>
</blockquote>
<p>一旦无法访问这些对象，要正确处理这些对象，首先需要识别它们。在识别循环的函数内部，维护了两个双向链表：一个列表包含所有要扫描的对象，<br>另一个将包含所有“暂时”无法访问的对象</p>
<p>GC启动时，它要扫描的所有容器对象都在第一个链表上。目标是移动所有无法到达的对象。由于大多数对象都是可达的，因此移动不可达的对象会更有效，因为这涉及更少的指针更新</p>
<h2 id="标记阶段"><a href="#标记阶段" class="headerlink" title="标记阶段"></a>标记阶段</h2><p>对象之间通过指针连在一起，构成一个有向图，对象构成这个有向图的节点，而引用关系构成这个有向图的边。从根对象[全局变量、调用栈、寄存器]出发，沿着有向边遍历对象，<br>可达的对象标记为活动对象， 不可达的对象就是要被清除的非活动对象。</p>
<h2 id="清除阶段"><a href="#清除阶段" class="headerlink" title="清除阶段"></a>清除阶段</h2><p>遍历所有对象，如果发现某个对象没有标记为“可达”，则就回收。</p>
<h1 id="分代收集"><a href="#分代收集" class="headerlink" title="分代收集"></a>分代收集</h1><blockquote>
<p>分代回收是建立在标记清除技术基础之上</p>
</blockquote>
<p>为了限制每次垃圾收集所花费的时间，GC使用了一种流行的优化：generations。这个概念背后的主要思想是假设大多数对象的生命周期都很短，<br>因此可以在创建后不久就被收集起来。这已被证明非常接近许多 Python 程序的实际情况，因为许多临时对象的创建和销毁速度非常快。<br>对象越旧，它变得无法访问的可能性就越小。</p>
<p>Python对象被分为三种世代:</p>
<ul>
<li>G0: 刚创建的对象</li>
<li>G1: 如果在一轮<code>GC</code>扫描中存活下来，则移至<code>G1</code>，处于<code>G1</code>的对象被扫描次数会减少</li>
<li>G2: 如果再次在扫描中活下来，则进入<code>G2</code>，处于<code>G2</code>的对象被扫描次数将会更少</li>
</ul>
<h1 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h1><ul>
<li><a href="https://devguide.python.org/internals/garbage-collector/">https://devguide.python.org/internals/garbage-collector/</a></li>
<li><a href="https://testerhome.com/topics/16556">https://testerhome.com/topics/16556</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/83251959">https://zhuanlan.zhihu.com/p/83251959</a></li>
</ul>
]]></content>
      <tags>
        <tag>垃圾回收机制</tag>
        <tag>Python</tag>
      </tags>
  </entry>
</search>
